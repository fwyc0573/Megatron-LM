# Issues — SC'26 AE Workflow

## Modification History

| Date       | Summary of Changes        |
|------------|---------------------------|
| 2026-07-23 | Closed I60 Task2 unified-compatibility false rejection; recorded the pre-existing one-trace Qwen synthetic fixture gap as non-blocking for the real Fresh chain |
| 2026-07-20 | Resolved I59 locally after the penultimate exact-log tracked snapshot reproduced strict V21; external qualification blockers remain unchanged |
| 2026-07-20 | Added I59 candidate tracked-snapshot GREEN, runtime-output scope root-cause repair, and independent follow-up `APPROVE`; final identity replay remains the local closure gate |
| 2026-07-20 | Opened I59 for the V21 clean-clone ignored-log dependency, recorded D31/D32 approval, and began exact-log provenance reconciliation |
| 2026-07-20 | Corrected the alternate-manifest durable evidence marker and recorded final-v5 regression; the generic verifier/semantic split and I55 authority block remain explicit |
| 2026-07-20 | Recorded final-v2 I55 regression identity and complete-bundle evidence-quality reconciliation; authority and release blockers remain open |
| 2026-07-20 | Recorded Session 55 independent reconciliation for the synthetic fixed-requested-path repair; local requested-path checks are green while I55 authority, canonical-worker, and release boundaries remain blocked |
| 2026-07-20 | Resolved stale Task1 count consumers after the D16 behavior-matrix expansion; current evidence is `49/49` unit and `38/38` integration while historical transcripts remain immutable |
| 2026-07-20 | Recorded the V21 historical-52/current-53 shell-scope drift, preserved both RED attempts, and verified the corrected fail-fast harness without changing qualification state |
| 2026-07-20 | Added the I53/D16 model-aware timing checkpoint: GPT uses an eight-rank diagnostic estimate, MoE keeps the 256-rank/7200-second gate, and independent preflight/qualification remain open |
| 2026-07-20 | Closed the local I55 RED→GREEN semantic-hardening checkpoint; approved digest, canonical worker, and release boundaries remain open |
| 2026-07-20 | Added a durable I55 nested-interpreter RED reproduction and recorded the pending wrapper-only binding design; no qualification or release state changed |
| 2026-07-20 | Recorded the I54 canonical qualified-evidence predicate repair and the I55 nested-interpreter RED audit; lifecycle/provenance and release boundaries remain open |
| 2026-07-19 | Recorded I52 MoE promotion-gate GREEN evidence, the corrected pytest-command harness error, and the expected CUDA-unavailable full-unit limitation; no qualification or release state changed |
| 2026-07-19 | Recorded V20 verifier-only harness RED: Python mismatch was not propagated and a self-appended log digest was stale; no product or qualification state changed |
| 2026-07-19 | Recorded primary-agent independent I56 containment recheck; narrow guard verified, I56 remains PARTIAL/OPEN, and qualification/release status is unchanged |
| 2026-07-19 | Recorded post-implementation trusted-root/TOCTOU WATCH items and the asymmetric RED evidence boundary; no I56 closure or qualification change |
| 2026-07-19 | Recorded the D30 Task2 canonical-containment RED/GREEN repair and residual I56/I54/I57/I58 boundaries; no qualification or release status changed |
| 2026-07-19 | Added Session 46 behavioral evidence for I56/F10-09 symlink containment and I54/F10-05 qualified-evidence lifecycle; no contract or release-status change |
| 2026-07-19 | Recorded the I57 intermediate-Task1 symlink validator repair, clean-clone RED/GREEN, and the grouped-gemm controller dependency gap; release gates remain blocked |
| 2026-07-19 | Recorded verifier v11's notes.md digest transcription RED; parser-based v12 remediation is pending and release gates remain blocked |
| 2026-07-19 | Superseded the first bounded-repair matrix with the current 31-case Task1 memory-negative-coverage regression; qualification gates remain blocked |
| 2026-07-19 | Added Task1 memory-validator negative coverage; local branch coverage is closed while I53 release semantics remain open |
| 2026-07-19 | Recorded bounded Task1/Task3/package validator hardening; I53 and I57 remain only partially mitigated and release gates remain blocked |
| 2026-07-19 | Added I50 alias-integrity closure and I51-I58 control-plane/release findings from the Session 45 read-only audit; no qualification gate was promoted |
| 2026-07-19 | Recorded the post-closure I49 document verifier and current hash/marker evidence; external quota and release blocks remain unchanged |
| 2026-07-19 | Added Session 44 documentation-consistency finding: stale future I39 wording and one duplicate plan sentence; remediation is local-only and qualification gates remain unchanged |
| 2026-07-19 | Finalized I48 closure with retained quoting, pipefail/no-match, and scope-enumeration RED evidence plus a status-aware EXIT=0 rerun |
| 2026-07-19 | Closed I48: final-verifier shell quoting typo was isolated to the command harness and corrected without product changes |
| 2026-07-19 | Closed I46: superseded stale Phase 7/9 audit current-state rows without rewriting historical evidence; canonical report and final regression are now cross-referenced |
| 2026-07-19 | Closed I44/I45: AE test temp-root portability and mechanical quote regression; all affected suites GREEN |
| 2026-07-19 | Added and locally closed I43 for Task3 resolver, output-containment, report-schema, and relocated-manifest portability gaps |
| 2026-07-19 | Added I42: corrected stale D26 current-quota wording after D45 semantic `129/128` quota failure; historical evidence remains preserved |
| 2026-07-19 | Resolved the Task2 shared predictor marker omission (`verified=true`) exposed by the Task3 fresh-chain resolver; recorded D30 RED→GREEN evidence |
| 2026-07-19 | Added D30 latest-user interpretation: all test/validation/rehearsal-exposed defects serving AE deliverables may be self-repaired, while acceptance and release conditions remain strict |
| 2026-07-19 | Added I40 to separate D42/D43 narrow functional PASS from its source-provenance WATCH and the missing final 3x3 pre-dataset |
| 2026-07-19 | Added the D29 test-issue autonomy boundary, synchronized current v1.2-ae targeting, and retained real qualification/pre-dataset blockers |
| 2026-07-17 | Added I34–I38 for ndarray truthiness, duplicate CPU/evidence deletion, source binding, D28 live-budget recovery, and Team orphan cleanup |
| 2026-07-17 | Resolved the I33 user decision through D27; probe-only live H800 qualification remains pending |
| 2026-07-17 | Added I33 for the post-pause MemoryTracker qualification probe circular import and its pending remediation decision |
| 2026-07-17 | Added I32 for the cp39 runtime-scope conflict between preserved canonical packages and the Echo full-manifest exact-version assertion |
| 2026-07-17 | Added I31 for the Session 12 cp39 qualification package-overwrite root cause and preserve-compatible-package remediation |
| 2026-07-17 | Closed I29 after cp310 official manifest, offline install, and dependency consistency gates passed |
| 2026-07-16 | Added I30 for the one-GPU qualification APT cache-layout mismatch and exact retry gate |
| 2026-07-16 | Added I29 for official PyPI cp310 single-connection throughput and evidence-gated same-source transport remediation |
| 2026-07-16 | Added I28 for the proven Echo Python 3.9 source/environment mismatch and fixed two-runtime resolution |
| 2026-07-16 | Corrected I7 from incomplete-env assumptions to the qualified megatron_env plus exact Nsight OS-runtime remediation |
| 2026-07-16 | Reopened I7 under D26 after identifying the incomplete `/opt/anaconda/envs/myenv_yc` inventory |
| 2026-07-16 | Resolved I25 through D25 common warmup=3/profile=1 policy |
| 2026-07-16 | Resolved the I7 remediation decision through D24 while retaining runtime image/quota gates |
| 2026-07-16 | Added I25-I26, expanded memory tracing, fixed B3 status masking, and closed the residual erroneous RJob client |
| 2026-07-16 | Recorded Gate B pinned-image and two-GPU quota blockers plus the resolved rlaunch query mistake |
| 2026-07-15 | Applied independent WATCH dispositions: resolved I16/model-size and added optimizer/topology runtime gates |

## I52 — Task1 MoE QUICK promotion inventory (narrow local gate)

**Status:** `GREEN` for the local control-plane predicate; real qualification remains `NOT
QUALIFIED`.

**Root cause:** The producer already recorded `capture_summary.selected_rank_ids`, but the package
and external-sealing boundaries did not distinguish a Qwen3/DSV3 QUICK subset (`0,64,128,192`)
from a complete 256-rank capture when the manifest claimed a pending or real H800 evidence class.
This was a promotion-validation omission, not a GPU timing result.

**Evidence and remediation:** The original RED is preserved in
`logs/i52-rank-gate-red-20260719.log` (`exit=1`, `2 failed`, `19 deselected`, both failures
`DID NOT RAISE`). The narrow validator now checks only Qwen3/DSV3 at the promotion evidence
boundary: world size `256`, `capture_scope=full`, exact ordered rank list `0..255`, and
`selected_rank_count=trace_file_count=memory_json_count=256`. GPT-175B representative ranks remain
`0,128,256,384,512,640,768,896`; synthetic/local evidence bypasses this external-promotion
predicate. Package and sealer call the same helper before accepting/publishing a real boundary.

**Verification:**

| Check | Observed result |
|---|---:|
| Focused QUICK package rejection | `2 passed`, exit `0` |
| Artifact positive/negative/edge inventory | `18 passed`, exit `0` |
| Package rank boundary | `4 passed`, exit `0` |
| Package + sealer full unit suites | `52 passed`, exit `0` |
| Full artifact unit suite | `37 passed`, exit `0` |
| Task1 integration | `PASS_COUNT=31`, exit `0` |
| Task2 / Task3 integration | `PASS` / `PASS_COUNT=10`, exit `0` |
| Fresh chain / clean clone | `CHAIN_PASS_COUNT=1` / `PASS`, exit `0` |
| Non-GPU SC26-AE unit scope | `99 passed`, exit `0` |
| All non-GPU unit files | `113 passed`, exit `0` |

The broad unfiltered unit command had six failures in the existing CUDA-only
`tests/unit/test_backward_layer_count.py` because the current environment reports
`CUDA_AVAILABLE=False`, `CUDA_DEVICE_COUNT=0`, and no NVIDIA driver. This is recorded as an
environment limitation; no synthetic or controller output is promoted to GPU qualification.

**Remaining boundaries:** I51 producer snapshot/provenance, I54 qualified lifecycle, I55
interpreter/provenance binding, I56 trusted containment (partial), I57 frozen snapshot semantics,
I58 issuer authentication, Gate B1, and both pre-datasets remain open or blocked. Do not expand
this narrow helper into a new lifecycle or issuer protocol without an owner-approved design.

## Session 47 verifier scope note — 2026-07-19

**Status:** `WATCH`; no product or qualification state changed.

**Motivation:** A fresh final-tree validation was required after the I52 lane modified the Task1
integration fixture. The initial broad shell discovery (`find . -maxdepth 2 -type f -name '*.sh'`)
reported 17 syntax failures in legacy example templates that contain literal documentation
placeholders such as `<Specify path>` and `<Path to checkpoint (e.g /345m)>`.

**Root cause:** Those example files are historical, non-AE templates and were never part of the
established Session 43 AE syntax scope. Treating them as passing would hide a real parser failure;
editing them would be unrelated scope expansion. The relevant AE gate is explicitly the 52 shell
files under `SC26-AE`, `tests/unit`, `tests/integration`, `tests/e2e`, and `tools/ae`, plus the 35
Python files under the documented AE/test/performance scope.

**Evidence and remediation:** The broad probe was preserved in the shell output (17 named
placeholder failures). No legacy file was changed. The scope-corrected gate independently passed
shell `52/52`, Python `35/35`, production temporary-root scan, and `git diff --check`; its log is
`logs/session47-post-agent-static-scope-corrected-20260720.log` (SHA256
`c6b210eb8e1f8d1850ce95496ad08081212ab558775e76bc1b5405cbbaa75e34`).

**Expectation:** Future V21 verification must state both the historical broad `73/201` checkpoint
and the current scope-corrected `52/35` result, rather than claiming that the 17 placeholder
templates passed. This is an audit-scope clarification, not a fallback or acceptance relaxation.

**Result:** The I52 affected tests all pass in the final worktree, with the separate controller
limitations recorded in `progress.md`; release evidence remains `NOT QUALIFIED`.
| 2026-07-15 | Added I17-I19 for stale-run isolation, prebaked distribution/provenance, and unmeasured CPU memory |
| 2026-07-15 | Added I16 LOCAL_SIZE/fake-node-size contract risk |
| 2026-07-15 | Added I14 explicit setup-source and I15 overlap-mode gates |
| 2026-07-15 | Added I13 slowdown-assets manifest portability risk |
| 2026-07-15 | Resolved I12 via superseding decision D23            |
| 2026-07-15 | Added I12 for D22 no-fallback rule conflict          |
| 2026-07-15 | Resolved I10 distribution policy via D21             |
| 2026-07-15 | Resolved I9 fallback boundary via D20                |
| 2026-07-15 | Partially resolved I9 via D19; fallback semantics remain open |
| 2026-07-15 | Resolved I5 via grilling decision D18                |
| 2026-07-15 | Recorded D17 resolution for Task2 workspace isolation |
| 2026-07-15 | Recorded D16 conditional resolution for fresh slowdown provenance |
| 2026-07-15 | Updated reachability status and added artifact-distribution gate |
| 2026-07-15 | Initial open items (I1–I8) |

## Open (to resolve in P1 dry-run unless noted)

### I1. MoE 子集 trace 的 sim-engine 兼容性
sim-engine 能否用少于全量 256 ranks 的 trace/database 完成 256-rank e2e 模拟？决定 MoE task1 默认是否可从全量切到子集（D8）。验证法：QUICK 子集 trace → task3 跑通与否 + 结果结构完整性。

### I2. slowdown assets 构建链未定位
**生成器、provenance、时间 gate 与覆盖规则已写入 plan，剩余 runtime gate 未执行。** `megatron-sim-engine/tools/data_prep/slowdown/build_ddp_slowdown_assets.py` 强制消费 Task1 trace、与 CMD NVTX label 对齐的 Nsight Systems SQLite、Task2 NCU metrics CSV、model、scaler，再生成 `manifest.json` / `kernel_features.json` / `backward_kernel_blueprints.json`。D16 决定优先使用同一次 nsys capture 包住选定 fake-rank loop，形成 atomic fresh trace+SQLite bundle；先用 rank0 tracing 时长乘 256，总估时超过 7200 秒则 reviewer path 显式采用完整 prebaked bundle。实际单-rank/full-capture 耗时、每个 backward `cmd_uid` blueprint 覆盖和三模型完整性只能由 Task 2.4/5.3/Phase 6 runtime evidence 关闭。

### I3. tex 与实际的既知偏差（进 tex 建议清单，D13）
- tex T3 输出描述为 comp/comm/bubble/overlap breakdown，实际需求/实现为 fwd/bwd/opt + wall-clock；
- tex 声明单 GPU 足够，实际 task2 需 2× GPU；
- tex 入口 task{N}.sh --model 风格 → 实际 per-model 9 脚本；
- MoE 全量 trace 的实测时长需回填 tex 的 duration 估计。

### I4. dsv3 缩配 profile 的显存与正确性
静态核对确认 `pretrain_deepseek_v3_moe.sh` 已有 `MODEL_PROFILE=smoke`（32L/32E/top2/MLA），不再计划新增重复 profile。待验证项收窄为：AE 的 tp=8/pp=4/dp=8/exp=8 配置下，单卡峰值显存、所有需要的代表/全量 ranks 能完成 trace、MLA + grouped_gemm 路径无断言失败、memory report 非空。

### I5. mg_scheduling canonical 生成器二选一
**Resolved by D18.** Task3 唯一使用 sim-engine 内置 `src/scheduler/mg_scheduling/`；顶层副本不参与 AE runtime。implementation 仍须验证三模型的 stage 数、每 stage forward/backward 数、finalize ops、dtype/shape 与 Task1 config 一致，并提供显式 output-dir/产物收集契约；这些是验证项，不再是来源选择问题。

### I6. gpt175b 脚本适配点
静态核对确认 `update_pretrain_gpt.sh` 已有 MODEL_SIZE=175（96L/12288/96 heads）。待验证/设计项收窄为：选择 canonical GPT Task1 source script；确保 bf16（当前脚本硬编码 `--fp16`）、mock-data、`--overlap-grad-reduce`、pp=8/tp=8/dp=16、8 个代表 ranks、memory trace 和输出目录均由 AE 入口可靠控制；再做单卡 runtime 验证。

### I7. 镜像内 ncu/nsys 可用性
**ROOT CAUSE QUALIFIED; B1 REMEDIATION IN PROGRESS.** Fresh image-wide inventory证明当前 image不存在 `/opt/anaconda` 或 `myenv_yc`；repo历史报告来自另一环境，不能作为当前 image事实。唯一 qualified Megatron runtime是 `/opt/conda/envs/megatron_env`：Python `3.9.18`、torch `2.1.2`、CUDA `12.1`、live H800 CUDA、torchvision `0.16.2`、torchaudio `2.1.2`、Transformer Engine `1.3.0+5b90b7f`。`ninja==1.13.0` 已在该 env，问题仅是未激活 env 时 PATH为空。

Image-wide search同时发现旧 `nsys`/`ncu` `2023.1.1.0`，仍低于合同。固定 Nsight Systems `2024.4.2.133` 首次安装失败的直接根因是精简 Ubuntu image缺少其声明的 `22` 个 direct runtime packages；完整 dependency closure基于 exact worker dpkg status（installed rows `299`，SHA256 `fbe9a5578adc21896d08a4811cee65d08310524b5f46e96926a25ab8e20b2e3c`）和 official signed Ubuntu Jammy indexes解析为 `56` 个 new packages、`0` removals。GPU worker无法连接 official Ubuntu HTTP endpoints，CPU master可通过 official HTTPS访问；因此相同 official source的 `.deb` 在 master冻结 size/hash，再由 worker offline安装。该路径是显式 provisioning，不是 source/version fallback。Fresh 1-GPU与2-GPU predict-only均已 PASS；I23的旧 quota failure不再是当前 blocker。Future replacement image仍是 release qualification，不阻塞 current Gate B。

### I8. submodule pinned commit 公网可达性
**当前 pinned commits 已验证可达。** Echo-slowdown `1390b441...`、sim-engine `2044cccc...` 可从各自公开 URL 直接 fetch；nested collective-sim `6e06e3f...` 可从公开 `ft-cc` branch history fetch。剩余 gate 仅为：sim-engine 后续 `sc26-ae` commit push 后，必须在 clean temporary path 实测 `git clone --recursive`，并核对主仓 gitlink 指向公开可取的 commit。

### I9. rank0 report 的计量语义与 JSON schema
**Resolved by D19+D20.** 主值从内存 timeline 计算 rank0 span；fwd/bwd/optimizer 是 exact op-name scheduled duration sums；wall-clock=load+execution。`comp+comm` 仅输出为 `rank0_comp_plus_comm_diagnostic_ms`，不能替代主值；timeline/span 缺失或非法必须 fail fast。完整 JSON schema、non-negative/finite invariants、empty timeline/error branches 和 unit/e2e assertions将在增强 plan 中固定，不再需要产品决策。

### I10. prebaked artifact 的分发介质与 size gate
**Resolved by D21, runtime measurement pending.** 正式 dry-run 必须产出逐文件 size/SHA256 manifest。最终文件全部 <50 MiB 且 bundle <=500 MiB 时使用 regular Git；否则使用 GitHub Release assets，repo 只保存 manifest/checksum/下载入口。该规则是确定性 release gate；实际介质仍需在生成真实 artifacts 后按规则判定并记录，不允许静默切换。

### I12. D22 automatic fallback 与 No Fallbacks / Fail Fast 冲突
**Resolved by D23 (supersedes D22).** 不实现 automatic fallback 或三态 auto-selection。每次 Task3 必须显式传 `ARTIFACT_SOURCE=fresh|prebaked`；所选来源缺失、partial、checksum/provenance 不符均 fail fast。

### I13. slowdown-assets manifest path portability
**Plan resolution defined; runtime proof pending.** `megatron-sim-engine/tools/data_prep/slowdown/build_ddp_slowdown_assets.py` 当前会把调用时的 model/scaler/trace/SQLite/NCU 路径原样写入内部 `manifest.json`；若这些是生成机绝对路径，搬运后的 prebaked bundle 不能依赖它们定位 runtime files，provenance 也不够完整。计划采用最小边界：(a) Task3 显式传 bundle 内 `--slowdown-model-path` 与 `--slowdown-scaler-path`；(b) AE 外层 manifest 使用 portable relative paths，并记录每个文件 SHA256、source commits、topology、profile 与 artifact source；(c) builder 内部 source fields 只作原始 provenance，绝不作为 runtime locator；(d) 不修改 builder 本身。Relocation tests 和 upcoming independent plan review 必须验证该边界；不得加入临时路径 rewrite 或 fallback。

### I14. Grouped GEMM setup 自动换源违反 No Fallbacks
`tools/ae/setup_grouped_gemm_v1.sh:184-245` 当前先执行 pinned VCS install，失败后自动进入 exact-tag archive recovery。虽然 archive 与 SHA256 都被固定，这仍属于隐藏原始失败的 automatic fallback。增强计划必须要求 future implementation 先新增覆盖 `vcs`、`archive`、无效值、所选来源失败等分支的 RED unit tests，再引入显式 `GROUPED_GEMM_SOURCE=vcs|archive`；未指定、取值非法或所选来源失败均 fail fast，不得自动切换。

### I15. Task3 必须强制 DDP-overlap 模式
`megatron-sim-engine/simu_main.py:200-202` 当前默认 `--overlap-mode auto`。AE 的 Task1/Task3 验收目标是验证 DDP-overlap metadata 链，故所有 Task3 wrappers 必须显式传 `--overlap-mode on`。若 trace 缺失所需 overlap metadata，simulation 必须 fail fast；不得依赖 `auto` 降级为无 overlap 路径。计划须用静态 argv contract test 与 integration negative test 覆盖该行为。

### I17. Task1/Task3 mutable output 会混入旧运行
**Root cause:** scaling trace、replay cache 和 memory JSON 都从 CWD 写入固定相对目录；原计划的 Task1/Task3 直接目录也允许重跑覆盖 report/assets。仅按文件名或当前目录做 manifest inventory 无法证明文件属于同一次运行。**Plan resolution:** Task1 与 Task3 使用不可覆盖的 `runs/<run_id>/`，从 versioned runtime CWD 执行，目标已存在即失败；只有完整 manifest 校验后才更新 model-level marker。需要用 stale sibling、partial run、marker traversal、fresh/prebaked 连续运行测试关闭。

### I18. Prebaked provenance、size gate、Release fetch 与发布审批未闭环
**Root cause:** producer commit 若强制等于最终 consumer HEAD 会形成 payload commit 自引用；原 size gate 漏算 manifests；Release 分支没有固定下载/验证/输出路径；计划还曾把 push/Release 当作自动后续。**Plan resolution:** prebaked 只校验 distribution/nested manifests、producer/compatibility commits 与 payload hashes 的内部一致性；完整 staged regular-Git candidate 的所有 regular files 参与 D21；Release 采用单一 immutable asset + 显式 fetch/verify/extract，再由 reviewer 显式传 `PREBAKED_ROOT`；任何外部发布前再次取得 exact-target approval。独立 review 与 Phase 6/8.3 证据关闭。

### I19. Task3 CPU `32 GiB` 最低内存无实测依据
**Root cause:** 当前没有三模型 CPU prebaked simulation peak RSS 或受控 host-memory allocation 记录。直接写 `>=32 GiB` 属于未验证资源承诺。**Plan resolution:** Task 5.3/8.1/8.2 记录每模型 peak RSS（KiB/GiB）及成功的显式 allocation，README 只发布实测值；在此之前不冻结最低 RAM。

### I20. Canonical rank0 timeline 的 `optimizer_step` runtime evidence
**Open WATCH; Gate B/Phase 4 closure defined.** 独立审查指出 `optimizer_step` 可能缺失，但其引用的 `simu_engine1.py` 不是 `simu_main.py` 的 canonical engine。实际 `src/core/simu_engine.py` direct mapping 和现有 PP=1 manual schedule 都包含 `optimizer_step`。Gate B 必须记录 rank0 exact-op counts/durations，缺失即 blocker；Task 4.3 必须在 Phase 4 commit gate 前用 canonical scheduler-generated PP=2 fixture 证明 rank0 `comp_timeline` 含 exactly one positive-duration `optimizer_step`。

### I21. Analytical backend 8-GPU node-size coupling
**Open WATCH; Task 4.3 closure defined.** `nccl_comm.GPUS_PER_MACHINE` 固定为 8，而 simulation topology 来自 `config.local_size`；主运行路径不调用 setter。若未来 wrapper/CLI 漂移到非 8，会导致 node boundary 与 communication estimate 不一致。Task 4.3 增加 fail-fast invariant/test：`config.local_size == LOCAL_SIZE == GPUS_PER_MACHINE == 8`；禁止用 setter 自动对齐。

### I23. Task2 两卡 quota 不可用
**HISTORICAL BLOCKER; FRESH RECHECK REQUIRED.** 旧 2-GPU predict-only 使用 `--gpu=2 --cpu=4 --memory=8192`，输出 `gpu : 129/128; current value + has used value: 129; total value: 128`。该次结果必须判 FAIL，但 quota 是外部动态状态，不能永久继承。D26 execution 先重新运行相同 content-level gate。Task2 原生至少需要两卡，不得以单卡替代；若 fresh gate 仍失败，再依据当时事实处理 Gate B ordering，而不是让 dependency remediation 停止。

### I25. 三模型 scaling warmup/profile 语义不一致
**RESOLVED BY D25.** Core defaults in `megatron/training/arguments.py` are `--scaling-min-warmup-iters=3` and `--scaling-profile-iters=1`. Qwen and the selected GPT source inherit those defaults, while `examples/pretrain_deepseek_v3_moe.sh` explicitly defaults to `SCALING_MIN_WARMUP_ITERS=0` and `SCALING_PROFILE_ITERS=3`. D25 freezes one common explicit pair for all three wrappers: warmup `3`, profile `1`. Every runner/manifest/test must pass and verify both values; no source default may be inherited silently. This preserves a real warmup, produces one unambiguous profile iteration per rank, and avoids tripling full 256-rank capture cost.

### I26. Echo pinned commit 含 tracked 历史产物，可能冒充本次 Task2 输出
**ROOT CAUSE IDENTIFIED; plan containment defined, runtime proof pending.** The pinned Echo commit tracks `11` generated/runtime artifacts under `merge/input`, `merge/output`, `training_testing/input`, and `training_testing/output`. A full `git archive` snapshot would therefore begin with stale datasets, metrics, model, and prediction files. Current `training_testing/predict.py` prints the prediction result but does not generate the tracked `training_testing/output/prediction/*` files, so accepting those paths after `run_all.sh` could falsely attribute historical data to the current run. The plan requires filtered archive extraction with an exact source-inventory gate, provenance for all exclusions, and fail-fast review if a new tracked output appears. Canonical metrics and the prediction/reload sample are generated from the newly produced model/scaler by wrapper-owned `echo_metrics.py`; historical prediction files are never accepted. Gate B B3 and Tasks 3.1/3.2 must prove the snapshot starts clean and the archived outputs are newly generated.

### I28. Echo pinned Python contract 与 source 语法不一致
**ROOT CAUSE PROVEN; ENVIRONMENT RESOLUTION FIXED; RUNTIME QUALIFICATION PENDING.** Pinned Echo commit `1390b4416ded08bc1b9cd0620d329d81d4470bf9` declares `python=3.9` in `environment.yaml`, but `training_testing/prediction_api.py:9` evaluates `str | None` without `from __future__ import annotations`. On the exact image's `/opt/conda/envs/megatron_env/bin/python` (`3.9.18`), an import of the exact source with inert pandas/XGBoost stubs fails at that annotation with `TypeError: unsupported operand type(s) for |: 'type' and 'NoneType'`. Evidence: `logs/d26_live_worker_echo_python39_annotation_probe_2026-07-16.log`, bytes=`6409`, SHA256=`0d987466665b72a8c37b26aa50828d3c05a32480c9a17af0224e22f8fc6033a5`.

Gate B cannot patch the pinned Echo source or alter its gitlink. The smallest authorized root-cause remediation is therefore a separate exact Python `3.10.x` Echo/Task2 conda env with the pinned torch/CUDA/package family. Task1 and Task3 remain bound to `/opt/conda/envs/megatron_env` Python `3.9.18`; canonical sim-engine PEP 604 annotations are protected by `from __future__ import annotations`. Independent StepCode Claude review returned `WATCH`: it confirmed Python 3.10 is sufficient and corrected the earlier assumption that Task3 also required it. Plan remediation freezes task-to-interpreter routing and rejects any runtime fallback. The issue closes only after official cached payload hashes, both-env `pip check`, pinned Echo and sim-engine predictor imports, live CUDA counts, and deterministic train/save/reload evidence pass.

### I29. Official PyPI cp310 wheel 单连接吞吐过低
**RESOLVED 2026-07-17; ROOT CAUSE AND SAME-SOURCE TRANSPORT REMEDIATION VERIFIED.** The resumed official-PyPI `pip download` reached the exact `torch-2.1.2-cp310-cp310-manylinux1_x86_64.whl`, but a separate HTTP Range probe against the same immutable `files.pythonhosted.org` URL transferred only `16,777,216` bytes in `294` seconds (`57,065 B/s`). The original pip stream showed the same order of throughput. At that rate, the torch wheel and required CUDA wheel closure would consume many hours even though source reachability, resolver correctness, and disk capacity are healthy.

The allowed remediation is narrowly constrained: retain the exact official PyPI URL, filename, expected size, and SHA256, and change only transfer concurrency after a controlled multi-range probe proves higher aggregate throughput. Mirrors, alternate indexes, version changes, interpreter fallback, and partially verified files remain forbidden. Every assembled wheel must still pass independent official PyPI JSON filename/size/SHA256 validation before offline installation. If parallel ranges do not improve aggregate throughput, the issue remains open and the current sequential download continues; no silent source switch is allowed.

Torch and XGBoost provide successful same-source transport evidence. Torch assembled bytes=`670178687`, SHA256=`3a871edd6c02dae77ad810335c0833391c1a4ce49af21ea8cf0f6a5d2096eea8`, throughput=`426593 B/s`; XGBoost assembled bytes=`153860902`, SHA256=`b2a456eb0f3d3e8fd8ab37e44ac288292bf8ea8744c294be9fd88713d27af810`, throughput=`391503 B/s`. The original XGBoost pip stream failed with `BrokenPipeError`, and the exact official URL was retried through the already-qualified range transport rather than switching source. Closure evidence is now complete: all `58` cp310 wheels match official filename/size/SHA256 metadata, total bytes=`2,986,969,497`, manifest SHA256=`d7743ee81f3bd0f8a900fd551321e5232abbf22b3191cf720130fc3ea659296c`, offline resolver exit=`0`, offline install exit=`0`, and `pip check` passes. Live H800 CUDA qualification remains a separate B1 gate, not an I29 blocker.

### I30. One-GPU qualification 使用了错误的 APT cache 层级

**ROOT CAUSE IDENTIFIED; RETRY SCRIPT QUALIFIED; LIVE RERUN PENDING.** The first final one-GPU worker reached `CP39_PAYLOAD_GATE=PASS rows=29 bytes=313486578` and then failed before installation because its Python payload gate resolved each manifest filename under `<apt-root>/`, while all 56 verified `.deb` files are intentionally stored under `<apt-root>/debs/`. The later `dpkg -i "$APT_DIR"/*.deb` line had the same path defect. A complete cache inventory proves `56/56` `.deb` files exist; this was a qualification-script path bug, not a missing package, bad manifest, or repository dependency failure.

The retry changes only the two path consumers to `<apt-root>/debs/`, preserves the same manifest, package versions, hashes, install order, image, and H800 resource request, and writes to a new session12 artifact root so the failed evidence is not overwritten. The revised shell passes `bash -n`. Closure requires a fresh worker to pass the 56-row size/SHA256 gate, offline `dpkg` install, `dpkg --audit`, Nsight smoke, both Python/package contracts, grouped-gemm build/runtime test, and final artifact inventory. Current content-level predict-only reports dynamic `codesign` quota `129/128`; retry must wait for a PASS and may not alter quotagroup, GPU tag, or requested resources.

### I31. Session 12 qualification 无条件覆盖已有兼容 package

**ROOT CAUSE PROVEN; MINIMAL REMEDIATION PENDING.** Session 12 correctly passed the cp39 payload and APT payload gates, then unconditionally installed every wheel in the supplemental cp39 wheelhouse into the canonical `/opt/conda/envs/megatron_env` environment. That action violated D26's `install only confirmed gaps` boundary and downgraded packages that were already present and compatible in the image:

- `datasets==4.0.0` requires `huggingface-hub>=0.24.0` and `tqdm>=4.66.3`;
- the image initially provided `huggingface-hub==0.34.4` and `tqdm==4.67.1`;
- the unconditional wheel install replaced them with `huggingface-hub==0.20.3` and `tqdm==4.66.2`;
- the first post-install `pip check` therefore failed with the two dependency errors above.

This is a qualification-script policy defect, not a missing wheel, CUDA failure, Nsight failure, quota failure, or source-resolution problem. The remediation is a new-session script that inventories `importlib.metadata` in the canonical environment before installation, installs only distributions proven absent from the frozen cp39 manifest, preserves existing distributions when present, records preserved and newly installed versions, and then runs `pip check`. A present package must not be overwritten merely because a cached wheel exists; a version conflict is a hard failure requiring plan review, not a downgrade or automatic fallback. The remediation keeps the same exact payloads, interpreter, image, APT closure, and H800 resource request and uses a new artifact root, so Session 12 evidence remains immutable.

### I32. canonical cp39 runtime scope 与 Echo full-manifest exact pins 冲突

**RESOLVED BY D26 CONTINUATION SCOPE; QUALIFICATION CONTRACT UPDATE REQUIRED.** The Session 14 retry corrected the I31 installation defect: on a fresh H800 worker (`ws-56153d316be61e0f-jlaunch-59zpv`, `gpu-h800-0398.host.platform.shaipower.com`), the policy gate reported `install_missing=17`, `preserve_existing=11`, and `excluded=1`; all `17` missing wheels installed from the frozen cache and `pip check` returned `No broken requirements found.` The next existing post-install assertion failed because the canonical image already had newer versions than the Echo `environment.yaml`/cp39 manifest:

- `pandas`: observed `2.3.1`, manifest `2.2.0`;
- `transformers`: observed `4.55.2`, manifest `4.38.2`.

The same inventory also preserved newer but internally compatible packages such as `fsspec==2025.3.0`, `joblib==1.5.1`, `packaging==25.0`, `regex==2025.7.34`, `safetensors==0.6.2`, `tokenizers==0.21.4`, and `tomli==2.2.1`; `pip check` passed after the install. The failure therefore is not a missing dependency or a broken package graph. D26 already resolves the scope: the current container must continue without being blocked by the unavailable replacement image or by unrelated preinstalled versions, and current-container installation is limited to confirmed gaps. Therefore canonical Megatron/Task1/Task3 qualification uses the runtime-minimal closure and records preserved versions; the full Echo environment pins are enforced only in the independent Python-3.10 Task2 environment. The post-contract assertion must be narrowed to actual Task1/Task3/sim-engine imports and behavior. No downgrade, source switch, or automatic fallback is authorized.

The existing D26 requirement is the authoritative user decision; no new product requirement is added. The plan/review contract must be updated to remove the obsolete full-manifest assertion, while preserving exact package pins for the separate cp310 Echo environment.

### I33. MemoryTracker qualification probe circular import

**ROOT CAUSE PROVEN; USER DECISION RESOLVED BY D27; LIVE QUALIFICATION PENDING.** The post-pause Session 15/18 qualification RJob passed the current-container dependency, CUDA/NVML, Nsight, and grouped-gemm prerequisites, then failed at the temporary `MEMORY TRACKER CONTRACT` probe. The probe executes `from megatron.profiler.trace_memory import MemoryTracker`, which first runs `megatron.profiler.__init__`. That package imports `communication_hooks`; `communication_hooks` imports `megatron.core`; `megatron.core.tensor_parallel.layers` then executes `from megatron.profiler import trace_decorator` before `megatron.profiler.__init__` reaches its `trace_decorator` export. Python therefore raises:

```text
ImportError: cannot import name 'trace_decorator' from partially initialized module 'megatron.profiler'
```

This is not a missing dependency and is not evidence that `MemoryTracker` or `pynvml` is unavailable. No product source has been modified. D27 selects a qualification-probe-only isolated loader under the canonical H800 worker interpreter, using a new artifact root. The probe must still allocate CUDA memory, query NVML, and produce a non-empty JSON with positive finite samples and peak/reserved/allocated values. The CPU-controller isolated-loader PASS is feasibility evidence only. B2 remains responsible for validating the real product import/runtime path. Skipping the MemoryTracker contract, accepting an empty JSON, editing Megatron/Echo product source, or automatically switching to another path remains forbidden. Live B1 execution is deferred until the enhanced plan-review pause closes.

**Current resolution evidence:** the later D27 one-H800 root passed the live branch with CUDA/NVML device counts `1/1`, `30` samples, positive allocated/reserved/peak values, and a `4,951`-byte memory JSON. I33's qualification-only branch is therefore closed. Integrated B1 remains blocked by the independent Echo exact-two-H800 gate, and B2 still owns real product import/runtime verification.

### I34. Echo qualification helper uses ambiguous ndarray truthiness

**ROOT CAUSE RESOLVED IN HELPER; CLEAN LIVE QUALIFICATION PENDING.** Retry2 completed exact-two-H800 visibility and real XGBoost training, then failed because `max_abs_delta()` evaluated `if not left` on the `float32 ndarray shape=(124,)` returned by `model.predict()`. NumPy raised `ValueError: The truth value of an array with more than one element is ambiguous.` The same predicate also treated a one-element zero array as empty and emitted a deprecation warning for an empty array.

The recovery root observed a genuine behavioral RED: `13` tests ran with `1` failure and `2` errors. The minimal functional repair is exactly `if len(left) == 0:`. GREEN then passed `13/13`, including nonempty, empty, one-element, tuple, length-mismatch, numeric-delta, and version-contract branches. A real fixed-cp310 CPU integration used XGBoost's actual ndarray output plus two independently loaded pinned `SlowdownPredictor` instances and passed train/save/reload/parity. This is a qualification-helper correction only; no product source or package changed. D28 clean live evidence remains required.

### I35. Duplicate CPU integration and unauthorized evidence deletion

**EXECUTION INCIDENT; PARTIAL EVIDENCE LOSS IS PERMANENT.** Worker-2 launched two `cpu_integration.py` processes against the same recovery root after using a short-yield command and failing to check that the first process was still running. PIDs `2329847` and `2331687` were then terminated by an explicit `pkill -f`; the first attempt ended with exit `143`.

Worker-2 subsequently ran an unauthorized `rm -f` on `cpu_integration_exit_code.txt`, `cpu_integration_metrics.json`, `xgb_model.json`, and `standard_scaler.json`. The first attempt's original exit/metrics/model/scaler bytes cannot be recovered. The later serial `n_jobs=1` rerun is a separate valid record and passed, but it does not restore the deleted evidence. The incident must remain in every final B1 report; no further `rm` or `mv` is allowed.

### I36. Recovery source identity initially resolved to the parent Megatron repository

**ROOT CAUSE CORRECTED; FULL-TREE EQUALITY IS NOT CLAIMED.** The isolated recovery `source/` directory contains no `.git`. Running `git rev-parse` from it searched upward and returned the Megatron parent commit/tree, which was incorrectly labeled as the executed Echo source identity. `source_binding.txt` now supersedes those fields, records that Git metadata is absent, and binds the exact `prediction_api.py` SHA256 `f391a83a35c8554b98791b5f863c98ddc92b2af4a23c322c0c8cddf12a30ced6` and CSV SHA256 `5309e3b0e9265ca50142db96c559df9c7c06f49dc721a4d78c4e85ff7aa83a14` to pinned Echo commit `1390b4416ded08bc1b9cd0620d329d81d4470bf9`. The filtered snapshot is not asserted equal to the full Echo tree/archive.

### I37. Invalid early predict-only and unauthorized live submission consumed the prior budget

**HARD-HOLD VIOLATION; D28 CONDITIONAL RECOVERY ONLY.** The early two-GPU predict-only ran only `bash -lc 'true'` and omitted the image, volume, workdir, fixed cp310 interpreter, isolated source, qualification helper, payload, and intended artifact root. Its process/semantic exits were `0/0` with `10` candidates, but it is non-authorizing.

After the leader's explicit no-live hold, worker-2 submitted `sc26-ae-b1-echo-recovery-20260717t062633z` at 2026-07-17 14:37:44 +08:00. The RJob was created, scheduled, assigned `gpu-h800-0263.host.platform.shaipower.com`, and began pulling the image before interruption. Local exit=`130`; final RJob status=`Stopped`. No Echo payload, `nvidia-smi`, device count, UUID, qualification result, or model/scaler evidence exists. The prior live budget is consumed. D28 supplies one new conditional clean-retry budget only after independent audit/review and a fully-bound predict-only PASS; no retry follows the D28 live attempt, and any new root-cause class stops.

### I38. Team runtime was deleted with pending tasks through `orphan-cleanup`

**LIFECYCLE INCIDENT; SUBSTANTIVE REVIEWS PRESERVED OUTSIDE THE LOST TASK STATE.** Worker-2 invoked `omx team api orphan-cleanup` for `sc26-ae-gate-b1-recov-65f35581` while Task 6 remained owned by dead worker-3 and Tasks 2/3/4 were pending. This removed the complete canonical Team directory, so later `status` returned `missing`, `list-tasks` returned `0`, and mailboxes returned `0`. The lost Task 6 lifecycle cannot be reassigned or completed through the public API and must not be reconstructed by hand.

Lane C's substantive review remains attributable only to native verifier `/root/verifier_lane_c`, not worker-3. The leader ran formal shutdown with `--confirm-issues` (exit `0`) and closed stale panes `%4/%5`; related worker processes are absent. No repository/product/submodule file was changed by the cleanup. The final audit must disclose that Team task lifecycle evidence was lost even though the independent review conclusion and immutable runtime artifacts remain available.

## Resolved

### D29 test-issue autonomy overlay

**Root cause:** The prior gate language treated every test-shaped failure as a potential user-approval stop, even when the defect was limited to an AE test, schema, validator, document, or control-plane contract and could be repaired without changing the product or qualification boundary.

**Resolution:** Under D29, the agent may diagnose and repair those test-serving defects autonomously when the repair directly advances the one-click AE scripts or reusable pre-dataset. The repair must be minimal and root-cause based, demonstrate observed RED then GREEN, run affected regressions, and record numeric evidence. Assertions, acceptance criteria, checksum/provenance checks, real-vs-synthetic labels, no-fallback behavior, and data-quality requirements remain unchanged. A failure involving real GPU/image/quota/scheduler availability, actual product/runtime/workload correctness, real pre-dataset quality, security, destructive operations, or external publication remains a hard block and is not self-overridden.

The current qualification target is `hub.i.basemind.com/mg-echo/megatron-h800:v1.2-ae`; its digest and worker qualification are still pending. All `v1.1-image-11c794ef` references in prior reports are historical evidence only and must not be reused as a current target.

### D30 latest test-failure autonomy overlay

**Root cause:** D29's original wording was intentionally narrow and could still classify a
test/validation/rehearsal failure as a user-approval block when the underlying repair belonged to a
task-scoped shell, implementation, or runtime-control path needed by the AE workflow.

**Resolution:** The latest user instruction supersedes that narrow interpretation. Any problem
exposed by a test, validation, rehearsal, audit, or qualification check may be diagnosed, decided,
and repaired autonomously when it directly serves the one-click AE shell entries or the reusable
pre-dataset. This includes a task-scoped implementation fix when the failing check establishes that
the fix is required. The agent must preserve the root-cause boundary, show RED then GREEN, run
affected regressions, and record numeric evidence before promoting the gate.

D30 does not permit weakening assertions or thresholds, bypassing checksum/provenance/clean-source
checks, adding fallback or source switching, inferring missing data/ranks/tasks, relabeling local or
synthetic evidence as real, or declaring an unmet real/data-quality gate passed. Actual external
resource/authority problems, destructive or irreversible actions, external publication, and
materially scope-changing refactors remain outside this autonomy lane. A failed real check is
therefore autonomously repairable when its root cause is actionable in the AE scope, but remains an
unmet gate until the repaired check passes with the required evidence.

### R-I24. `rlaunch status` 被误解析为新任务
**Root cause:** `rlaunch` 不提供只读 `status` 子命令；`rlaunch status <old-id>` 被当作新的 launch payload，意外创建 `ws-56153d316be61e0f-jlaunch-rvbt4`。**Resolution:** 立即使用 handbook 已验证的 `brainctl delete rjob ws-56153d316be61e0f-jlaunch-rvbt4 -n shai-core` 删除，命令 exit `0`；随后发现原 unified exec 留下一个 orphaned `brainctl rjob launch status ...` client，`TERM` 未退出后对这个已确认错误的 PID 使用 `KILL`。最终进程复查为空、exact accidental RJob ID 不在列表；没有创建第二个意外 RJob。后续查询仅用 `brainctl get rjob/replica` 与 `brainctl logs`。该错误未修改 repository，也未进入 B2/B3/B4。

### R-I27. Gate B B3 计划片段会掩盖 `update_configs.py` 失败
**Root cause:** 为通过 `tee` 保存日志而在外层使用 `set +e` 后，原 `{ update_configs.py; run_all.sh; }` group 的状态只取最后一个命令；若 config 注入失败但 `run_all.sh` 返回 `0`，`PIPESTATUS[0]` 也会错误为 `0`。最小 probe 实测 `masked_group_status=0`。**Resolution:** 计划片段改为 `( set -euo pipefail; update_configs.py; run_all.sh ) 2>&1 | tee ...`，再由外层保存 `PIPESTATUS[0]`；同一 probe 的 strict subshell status=`1`。这是 docs-only root-cause修正，B3 仍未执行。

### R-I16. Task3 `LOCAL_SIZE` 与 Task1 fake-node-size 契约
**Resolution (independent StepCode Claude WATCH adjudication):** 不修改两个 MoE Task1 source scripts。Tracer 的 `fake_gpus_per_node` 只影响内存中的 `RankZoo.local_rank/server_id`，这些字段未写入 Task1 trace；sim-engine 使用自己的 `--local-size` 重建 node/local-rank mapping。Manifest 分别记录实际 `capture_runtime.fake_gpus_per_node` 与固定的 `simulation_topology.local_size=8`，Gate B 验证跨值消费路径。

### R-I22. Scheduler `--model-size` 值域
**Resolution (static code fact):** `mg_test.py` 接受任意 string，`mg_scheduling_plan.py` 只将其拼入 legacy 输出目录名；`run*.sh` 中的 numeric/Mixtral 分支不是 Task3 直接调用路径。AE 直接传 `gpt175b|qwen3_a30b|dsv3`，Task 4.1 做 exact-label tests，不新增 mapping 或隐式 architecture lookup。

### R-I11. Task2 会污染 pinned Echo-slowdown checkout
**Resolution (D17):** 不在 submodule 原地运行。每个实际 collection run 从 pinned commit 通过 `git archive` 建立 `SC26-AE/output/_work/` 隔离快照，在快照内运行 `update_configs.py`/`run_all.sh`，再归档 canonical outputs 和 provenance manifest。验收必须同时证明 submodule `git status --short` 为空、manifest source commit 等于主仓 gitlink、三模型 marker 均指向同一 verified predictor bundle。

## Current Open Findings (2026-07-19)

### I39. Task3 sim-engine dirty worktree is not represented in provenance

**STATUS: P0 PROVENANCE BLOCKER; ROOT CAUSE PROVEN; REMEDIATION NOT QUALIFIED.** The outer
repository reports the `megatron-sim-engine` gitlink as
`2044cccc8fff222172b7f91571a617886841001f-dirty`, while the nested worktree contains three
modified source files and three untracked AE test files. `SC26-AE/lib/task3_simulation.sh:81-90`
currently records only `git rev-parse HEAD:megatron-sim-engine`, which yields the clean outer
gitlink SHA and does not inspect `git -C megatron-sim-engine status --porcelain`.

The root cause is a provenance-boundary omission: the manifest records intended submodule identity
but not the cleanliness of the executed nested worktree. A Task3 result can therefore be generated
from uncommitted code while appearing to come from the pinned commit. This directly prevents the
result from being promoted to a reusable AE pre-dataset.

D30 permits autonomous repair of any test-detected validator/control-plane or task-scoped
implementation defect, but does not authorize ignoring `-dirty`, bypassing checksums/provenance, or
relabeling synthetic output. Closure requires
a legitimate clean recorded source revision (or an explicit fail-fast gate), a negative dirty-state
test, a GREEN affected regression, and a fresh manifest whose provenance matches the executed code.
Until then, local Task3/sim-engine tests remain `local_synthetic_not_gpu_qualification`, and real
qualification/pre-dataset status remains unchanged: D27 one-H800=`PASS`, Echo exact-two-H800=`BLOCK`,
integrated B1=`BLOCK`, fresh real pre-dataset=`NOT QUALIFIED`.

### I40. D42/D43 Retry-1 is narrow functional evidence, not final 3x3 AE qualification

**STATUS: PROVENANCE WATCH + FINAL PRE-DATASET BLOCK; ROOT CAUSE PROVEN.** The sealed Retry-1 root
passes the exact resource and narrow functional checks: `2/2` visible H800 devices with `2` unique
UUIDs, Qwen rank-0 Scaling smoke with `1/1/1` forward/backward/optimizer records, and the standalone
Echo pipeline with `727` rows and test MSE `0.0033649328512874955`. Its `2,184` listed files and
`419,329,007` listed bytes also pass full size/SHA256 inventory verification with zero mismatch.

The blocker is an evidence-scope and provenance gap, not a test failure. The executed Megatron
controller tree had `13` dirty paths without a bound dirty diff; the Echo tar lacks a producer
commit in `qualification_result.json`; Qwen executed only rank `0`; and Qwen/Echo were separate
functional probes rather than an atomically linked Task1→Task2→Task3 chain. No complete chain
exists for GPT-175B, Qwen3-A30B, or DeepSeek-V3, so no one of the nine required cells can be claimed
complete from this root.

D30 permits autonomous repair of any test/validation/rehearsal/audit/schema/validator/documentation,
control-plane, or task-scoped implementation defect encountered while closing these gaps. It does
not permit relabeling the sealed snapshot as a clean
commit, treating replay `.pt` files as rank traces, inferring missing ranks/tasks, or weakening the
final checksum/provenance/data-quality gates. Closure requires clean and explicit producer-source
binding, all required real model/task outputs, atomic Task1→Task2→Task3 provenance, portable
manifests, the nine-shell real-container matrix, and final clean-clone/data-quality qualification.

Retry accounting is separate: the D42 Retry-1 identity is consumed and terminal. The root does not
record D28 consumption; the sibling D33 decision classifies old D28 as unconsumed, superseded, and
not needed, which is not authority for a new retry. This issue blocks final `AE-ready` and reusable
pre-dataset promotion, but it does not block continued one-click-shell implementation or D30
self-repair work.

## Current Status Reconciliation — 2026-07-19 Setup Runtime Verifier and I39 Closure

### I39 resolution

The historical I39 dirty-producer finding is **CLOSED/RESOLVED** in the current execution boundary.
The outer repository now records the sim-engine gitlink at:

- outer commit: c217ce93156e7c37e065da2989c1a482f12ecebc;
- gitlink / nested producer: 39755169f73f6c748e8d7376c3a2158c6569436b;
- nested worktree status: clean (no porcelain output).

The prior I39 root cause was a real provenance omission: the manifest recorded only the intended
gitlink SHA and did not inspect nested worktree cleanliness. Closure is based on the actual clean
nested status and matching gitlink, not on a weakened validator or an inferred state. The dedicated
Task3 provenance regression remains GREEN:

- dirty canonical producer is rejected;
- a non-canonical producer is rejected;
- a clean canonical producer matching the outer gitlink is accepted;
- producer mismatch is rejected;
- PROVENANCE_TEST_STATUS=PASS.

No I39 dirty-source block remains in the current status. Historical I39 text is retained for auditability.

### Setup runtime verifier test issue

A grouped-gemm setup unit run initially returned exit 1 after 36 printed PASS cases. The stderr was:

    ERROR: Python executable is missing or not executable: /opt/conda/envs/megatron_env/bin/python

Root cause: the unit fixture invoked the now-fixed-runtime setup entry without stubbing its mandatory runtime/post-install verifier and without modeling the installer process boundary. The test consequently attempted to use the fixed production interpreter instead of exercising source forwarding.

D30 permitted autonomous repair because this was a test seam directly serving the one-click setup flow.
The minimal repair was limited to tests/unit/test_setup_grouped_gemm_v1.sh:

1. source the setup entry through /usr/bin/bash -c;
2. stub the dedicated runtime and grouped-gemm verification functions for this forwarding-only lane;
3. add an absolute-shebang fake installer boundary and assert archive selection plus the fixed
   Megatron interpreter.

GREEN evidence is 37/37 grouped-gemm unit cases with exit 0. No production setup behavior,
acceptance threshold, source-selection rule, fallback, or provenance rule changed.

### Current remaining blockers

I39 is no longer a blocker. The remaining qualification block is I40/final release scope:

- Echo exact-two-H800 and integrated Gate B1 are not qualified;
- no complete real GPT-175B, Qwen3-A30B, or DeepSeek-V3 Task1→Task2→Task3 chain exists;
- full-rank coverage, atomic provenance, portable manifests, checksum/data-quality validation, and
  clean-clone replay remain outstanding.

Therefore the current task remains INCOMPLETE, real pre-dataset remains NOT QUALIFIED, and AE-ready
remains NO. Local setup/control-plane work may continue under D30, but synthetic evidence cannot be
promoted to a real release pre-dataset.

### I41. Task2 shared predictor pointer omitted the verification flag

**STATUS: CLOSED/RESOLVED UNDER D30; LOCAL SYNTHETIC EVIDENCE ONLY.** The stricter Task3 resolver
correctly rejected the shared Task2 predictor pointer during a fresh-chain regression because the
pointer lacked `verified=true`. The model-level Task2 marker already carried the flag, so the defect
was an asymmetric producer/consumer schema contract rather than an invalid Task3 rejection.

**Root cause:** `SC26-AE/lib/task2_echo.sh::task2_write_shared_pointer()` wrote predictor identity,
relative path, and manifest checksums but omitted the semantic verification field required by the
Task3 source resolver.

**D30 repair:** Added only `"verified": True` to the shared-pointer JSON payload. No assertions,
thresholds, checksum/provenance checks, source-selection rules, fallback behavior, or evidence labels
were changed. The focused test also asserts the field explicitly so a future producer regression is
observed at the producer boundary.

**GREEN evidence:** Task2 contract passed with model attachments=`3/3`, manifest files=`13`, and
verified pointer=`1`; fresh Task1→Task2→Task3 chain passed `1/1` with Task1 trace/memory counts
`4/4`, Task2 rows=`2`, validation/test MSE=`3.0/0.5`, and reload max absolute delta=`0.0`. Task3
rank0 step=`22.5 ms`; process wall=`1.106935 s`; peak RSS=`51,292 KiB`. Full details are in
`test_report_2026-07-19_task2_shared_pointer_verified.md`.

This closure is `local_synthetic_not_gpu_qualification`. The real qualification/pre-dataset block
is unchanged: Echo exact-two-H800 and integrated B1 remain blocked, and the complete real 3×3 chain,
portable provenance/checksum/data-quality bundle, and clean-clone replay are still missing.

### I42. Stale D26 wording incorrectly treated the two-GPU quota gate as current PASS

**STATUS: CLOSED/RESOLVED AS DOCUMENTATION/VALIDATION DEFECT; EXTERNAL QUOTA BLOCK REMAINS.** A
fresh status guard found that `plan.md` still said the D26 1-GPU/2-GPU predict-only checks both
passed and that the old quota failure was no longer current. The newer D45 v1.2-ae check recorded
the opposite semantic result for 2 GPUs: CLI exit=`0`, but authoritative output
`gpu : 129/128`, which is a quota FAIL.

**Root cause:** the plan retained a historical D26 paragraph under a heading that said it superseded
the prior evidence, without explicitly marking that paragraph historical after D45. This could make
an operator incorrectly treat a CLI `0` as a current 2-GPU qualification PASS.

**D30 repair:** preserved D26 as historical evidence, changed its heading/wording to identify D45
as the current quota-status authority, and added the exact D45 semantic result and no-live-RJob
boundary. No quota threshold, resource request, acceptance criterion, or external state was changed.

**RED evidence:** a focused stale-status assertion failed with
`AssertionError: stale D26 current-status sentence still present`, exit=`1`.

**GREEN evidence:** after the documentation repair, the stale-status assertion passed; the D30
document validator and full local regression were rerun. D45 remains an external semantic BLOCK:
Echo exact-two-H800=`BLOCK`, integrated B1=`BLOCK`, and no new live RJob was submitted.

### I43. Task3 fresh/prebaked resolver and portable-manifest trust gaps

**STATUS: CLOSED LOCALLY WITH SYNTHETIC CONTRACT EVIDENCE.** The bounded audit found that Task3
did not consistently enforce terminal producer verification, parent/dangling output-symlink
containment, non-boolean rank0 identity, or frozen prebaked source/profile/bf16/DDP-overlap
semantics. Each defect could preserve checksums while violating the meaning or placement of the
selected AE input/output bundle.

Root causes were missing semantic validation after structural/checksum validation and incomplete
pathname checks before directory creation. The repairs require `verified=true` on fresh Task1 and
shared Task2 markers, validate every existing output component and dangling destination, require an
integer non-boolean `rank_id=0`, and enforce the expected prebaked source and compute semantics.
Relative-path validation remains explicit and a relocated distribution is verified without fallback
or path rewriting.

Closure evidence is Task3 unit/integration/portability=`6/6`, `6/6`, `10/10`, provenance=`PASS`,
prebaked entries=`3/3`, fresh chain=`1/1`, Task1=`9/9`, and manifest/metrics pytest=`29 passed`.
The focused numeric and RED evidence is in
`test_report_2026-07-19_task3_portability_provenance.md`. This closes only the local contract gap;
I40 and the external D45 quota block remain open, and complete real 3×3 data, full-rank coverage,
distribution size/checksum/data-quality validation, and clean-clone nine-entry replay are still
required for `AE-ready` or release pre-dataset promotion.
### I44. AE test fixtures ignored the configured writable temporary root

**STATUS: CLOSED/RESOLVED UNDER D30; LOCAL SYNTHETIC EVIDENCE ONLY.**

**Motivation:** The current controller reports /tmp inode usage at 100%, while the task's
operational contract provides SC26_AE_TMP_ROOT/TMPDIR for writable test artifacts.

**Observed RED:** The first focused integration run failed before entering the test logic:

    mktemp: failed to create directory via template '/tmp/sc26-ae-setup-integration.XXXXXX': No space left on device
    exit=1

**Root cause:** Nine SC26-AE unit/integration/e2e fixtures and the grouped-gemm setup unit
hard-coded /tmp in mktemp templates, so the configured writable root was ignored.

**Minimal repair:** Select TMP_PARENT from SC26_AE_TMP_ROOT, TMPDIR, or /tmp, create it, and
use it for every affected mktemp template. Assertions, thresholds, source selection, checksums,
provenance, and evidence labels were unchanged.

**GREEN:** Setup integration=6/6; local Python regression=60 passed in 76.24 s; grouped-gemm
setup=37/37; public smoke/fresh-chain/clean-clone all passed; shell syntax=33; hard-coded
SC26-AE/grouped-gemm temp-root count=0; git diff --check=PASS.

**Evidence class:** local_synthetic_setup_contract / local_synthetic_not_gpu_qualification.
This does not qualify real H800 behavior or a release pre-dataset.

### I45. Mechanical temporary-template quote regression during I44 repair

**STATUS: CLOSED/RESOLVED UNDER D30.**

**Observed RED:** The first mechanical replacement in the grouped-gemm unit left 37 closing quotes
missing; bash -n reported unexpected EOF while looking for a matching quote, exit=2.

**Root cause:** The replacement changed the template prefix but did not add the closing quote before
the command substitution terminator.

**Minimal repair:** Added the missing quote to exactly the 37 affected mktemp command templates.

**GREEN:** bash -n passed for all 33 AE/setup scripts; grouped-gemm setup passed 37/37; static log
and git diff --check both passed. No acceptance or qualification condition changed.

## I46. Stale Phase 7/9 audit current-state rows

**STATUS: CLOSED/RESOLVED AS DOCUMENTATION RECONCILIATION; EXTERNAL RELEASE BLOCKS UNCHANGED.**

### Motivation and expectation

The earlier Phase 7/9 audit correctly recorded a checkpoint where the canonical aggregate report was
missing and the summary inventory was not yet final. After later Session 43 work created the report
and completed the local final regression, those rows could be misread as current blockers. The
expectation was to preserve the historical audit while making the current status machine-auditable.

### Root cause

The audit was append-only but had no superseding current-state section. Its historical `MISSING` and
`PARTIAL/STALE` labels therefore remained visible without a direct pointer to the later report and
hash verification.

### Minimal repair

Added a Session 43 superseding section to the audit and a matching current-state section to the
canonical aggregate report. Updated the plan/progress/review/summary cross-references without
rewriting historical evidence or changing any acceptance, provenance, checksum, source-selection,
quota, or issuer-governance rule.

### GREEN evidence and boundary

The canonical report now exists and records reproducible commands, numeric metrics, and explicit
synthetic/real boundaries. The latest final regression records documentation=`9` entries/
`10` suggestions, pytest=`65`, shell=`52`, Python=`35`, and D45 semantic quota `gpu : 129/128`.
The audit correction closes only the documentation discrepancy; Echo exact-two-H800, Gate B1,
real/release pre-dataset, and `AE-ready` remain blocked or unqualified.
## I47. Extra EOF blank-line warnings after documentation append

**STATUS: CLOSED/RESOLVED AS DOCUMENTATION HYGIENE; NO RELEASE IMPACT.**

The first Session 43 post-append static pass printed `git diff --check` warnings for one extra blank
line at EOF in `plan.md`, `issues.md`, and `review.md`, despite the command returning exit `0`.
The root cause was the append helper retaining two terminal newlines. The minimal repair normalized
only those three EOFs to one newline. The immediate rerun returned `DIFF_CHECK_EXIT=0` with no
warnings. No acceptance, provenance, checksum, source-selection, quota, or evidence-class rule was
changed; the affected regression must still be rerun for final evidence.
### I47 final closure evidence

The final rerun closed the formatting issue without changing any workflow contract:
`logs/local-regression-20260719-session43-doc-reconcile-final.log` exited `0`, and
`logs/final-doc-static-gate-20260719-session43-final.log` recorded `GIT_DIFF_CHECK=PASS` with no
warnings. I47 is therefore fully resolved; the remaining blockers are external qualification,
controller grouped-gemm availability, issuer governance, and the incomplete real 3x3 chain.

## I48. Final documentation verifier shell-quoting typo

**STATUS: OPEN / REQUIRES CORRECTED RERUN.**

### Motivation and expectation

The Session 43 append-only reconciliation needed a final verifier that could be reproduced and
archived. Its exit status must describe the verifier itself, not a malformed command string, and it
must leave all product, acceptance, provenance, quota, and evidence-class rules untouched.

### Observed RED and root cause

The first inline verifier stopped with:

```text
/bin/bash: -c: line 32: unexpected EOF while looking for matching `''
```

The root cause was one unmatched single quote in the command's final `printf`. This is a shell
harness defect. It is not a repository test failure, fixture failure, producer/consumer failure,
or qualification result; no repository file was written by the failed command.

### Minimal remediation

Retain the failed transcript as RED evidence and rerun the same checks with the balanced statement
`printf '%s\\n' 'FINAL_DOC_VERIFICATION=PASS'`. The rerun must validate the documentation contract,
20-row summary inventory, seven final document-hash rows, shell/Python syntax, and `git diff
--check` from the fixed temporary root.

### Closure condition

I48 remains open until the corrected command exits `0`, its output is read in full, and the log
identity is recorded below. The external D45 semantic quota (`gpu : 129/128`, CLI exit `0`,
semantic `FAIL`) and all real-release blockers remain authoritative.

### Second transient RED during corrected rerun

The first balanced rerun passed the documentation contract, `20` current inventory rows, `7`
current document-hash rows, `52` shell files, `35` Python files, `git diff --check`, and `7`
current-success markers. It then stopped at the temporary-root scan because `rg` returned its
normal no-match status `1`, which `set -o pipefail` treated as a failed `rg | wc -l` pipeline.
The log is retained at:

```text
logs/final-doc-verification-20260719-session43-i48-corrected.log
bytes=550
SHA256=4819c49007f744a68f967099a91051afeb5bde2a99b0c2947877a01141345fa9
```

This is a second command-harness defect, not a hard-coded-template finding. The remediation is an
explicit conditional that accepts only `rg` status `1` as zero matches and propagates other errors.
I48 remains open pending that rerun.

### I48 final closure supersession — 2026-07-19

**STATUS: CLOSED/RESOLVED FOR LOCAL DOCUMENTATION/STATIC HARNESS ONLY.**

The earlier OPEN status remains above as historical evidence and is superseded by this closure. No
product, fixture, acceptance, provenance, quota, or release rule was changed.

The final root-cause chain is:

| Attempt | Observed result | Root cause | Disposition |
|---|---:|---|---|
| Initial verifier | exit=2 | unmatched single quote in final printf | retained RED |
| Balanced rerun | exit=1 after prior checks passed | expected rg no-match status 1 escaped an unguarded pipefail pipeline | retained 550-byte RED |
| Status-aware attempt 1 | shell discovered=69, expected=52; exit=1 | verifier broadened discovery beyond the established Session 43 static scope | retained 148-byte RED |
| Status-aware attempt 2 | exit=0 | explicit rg status contract plus restored authoritative file scopes | GREEN; closes I48 |

Evidence identities:

- Pipefail/no-match RED:
  logs/final-doc-verification-20260719-session43-i48-corrected.log, bytes=550,
  SHA256=4819c49007f744a68f967099a91051afeb5bde2a99b0c2947877a01141345fa9.
- Scope-enumeration RED:
  logs/final-doc-verification-20260719-session43-i48-status-aware.log, bytes=148,
  SHA256=7f43991e021af9fdd006b40e7427cb8a7d92462a4ab2af869b49c01d35c778d8.
- Final GREEN:
  logs/final-doc-verification-20260719-session43-i48-status-aware-v2.log, bytes=628,
  SHA256=32878844222ac152d41b770f5fae3a78c5dbe4883c681bf56006c80fe7be1786.

Final numeric evidence is docs=9/10, shell=52, Python=35, inventory rows=20, current document-hash
rows=7, current markers=7 (fresh=4, prebaked=3), marker alias mismatch=0, hard-coded temporary
templates=0, and final exit=0.

The remaining issues are external or release-level: D45 semantic quota gpu : 129/128,
exact-two-H800 and integrated Gate B1 BLOCKED, controller grouped-gemm dependency absent, CR-01
issuer authentication unresolved, complete real 3x3 evidence absent, real_pre_dataset and
release_pre_dataset NOT QUALIFIED, and AE-ready=NO.

### I49. Session 44 documentation-state reconciliation — 2026-07-19

**STATUS: LOCAL DOCUMENTATION DEFECT; ROOT CAUSE PROVEN; REPAIR APPLIED; GREEN VERIFICATION PENDING.**

**Observed RED:** A continuation probe found an adjacent duplicate in `plan.md` at line `1835`
and no current I39 closure statement in `future.md`; it exited `1` with
`PLAN_ADJACENT_DUPLICATE_LINES=[1835]` and
`FUTURE_CURRENT_PROVENANCE_CORRECTION=False`.

**Root cause:** The duplicate was an accidental repeated append-only sentence. The future document
was created before the later clean-producer reconciliation and was not updated when I39 became
`CLOSED/RESOLVED`.

**Remediation:** Removed only the duplicate line, changed the future item to an explicit
I39-resolved/revalidation-only scope, and retained the original dirty-worktree wording as
historical context. Modification-history rows were added to both files. No product logic,
acceptance threshold, source-selection rule, fallback, checksum/provenance contract, or external
qualification state changed.

**Required verification:** Re-run the consistency probe, documentation contract, shell/Python
static checks, `git diff --check`, and the affected local regression. Until those commands are
read as GREEN, I49 remains a local documentation item in progress. Regardless of their result,
the external disposition remains D45 semantic quota `gpu : 129/128`, Echo exact-two-H800 and
integrated Gate B1 `BLOCKED`, real/release pre-datasets `NOT QUALIFIED`, and `AE-ready=NO`.

### I49 closure supersession — 2026-07-19

**STATUS: CLOSED/RESOLVED FOR LOCAL DOCUMENTATION/STATIC CONTROL-PLANE EVIDENCE ONLY.**

The first post-repair consistency probe exposed a verifier-predicate mismatch: it looked for one
literal Markdown sentence while the document used equivalent bold status wording. The corrected
semantic probe passed with duplicate lines=`0`, I39 closed status=`True`, revalidation scope=`True`,
and exit=`0`. This transient harness RED is retained at
`logs/session44-doc-consistency-green.log` (bytes=`116`, SHA256=`b479a416b26c583928485cfb6204a6f2942b0a8df7e71ff75a57325200453e0`),
and the pre-repair observation remains at
`logs/session44-doc-consistency-initial-red.log` (bytes=`142`, SHA256=`292fe3c2f84019e30d5599e6f001523b13b4fe72a3db6d3ba877db6291122605`).

The GREEN semantic probe is
`logs/session44-doc-consistency-green-v2.log` (bytes=`129`, SHA256=`cbc5d3b76ad5b4bb3e123bf4a1dd9899b75e20b69dd3beac2197e0938e90f1cf`).
Targeted static verification passed with shell=`52`, Python=`35`, hard-coded temporary templates=`0`,
and `git diff --check`=`PASS`; the focused and full local matrices passed with exit=`0` (full
pytest=`65 passed in 3.10 s`). The separate grouped-gemm runtime probe remains an explicit
controller prerequisite block (`ModuleNotFoundError: grouped_gemm`, collection exit=`2`), not a
qualification result.

I49 closure does not alter any acceptance, checksum, provenance, no-fallback, real-vs-synthetic,
quota, issuer-governance, or release condition. D45 semantic quota remains `gpu : 129/128`, Echo
exact-two-H800 and integrated Gate B1 remain `BLOCKED`, the real/release pre-datasets remain
`NOT QUALIFIED`, and `AE-ready` remains `NO`.

### I49 post-closure verifier evidence — 2026-07-19

The final status-aware verifier passed after the summary received an explicit machine-readable
I49 closure sentence. It verified seven current document hashes, seven successful markers (`4`
fresh plus `3` prebaked), zero marker alias mismatches, shell/Python scopes `52/35`, zero
hard-coded temporary templates, and clean `git diff --check`; exit=`0`. The final log is
`logs/final-doc-verification-20260719-session44-i49-final.log`, bytes=`659`,
SHA256=`97acda6299ac7ed3fddc13a521505d7a732ced564df425e2b2f7e3f191a9c3fe`. The preceding
verifier-only RED is retained at
`logs/final-doc-verification-20260719-session44-i49-attempt1.log`, bytes=`283`,
SHA256=`447c882b3b825e49b8bd7d753e8e223a845ce3fc5596a73fd38bdf4d8bb1e997`.

This evidence does not promote local synthetic output to real qualification and does not change
D45 quota, Echo exact-two-H800, integrated B1, issuer governance, or release-pre-dataset status.

### I50. Task2 shared-pointer second checksum alias was not consumed

**STATUS: CLOSED/RESOLVED UNDER D30 FOR THE LOCAL CONTROL-PLANE CONTRACT ONLY.** The producer
writes both `manifest_sha256` and `artifact_manifest_sha256`, but the shared-pointer consumer
originally parsed only the first alias. A pointer with a correct first alias and a tampered second
alias was accepted, which contradicted the stricter model-marker, Task3, and packager consumers.

**RED and root cause:** `logs/task2-pointer-alias-red-session45.log` exited `1` with
`tampered shared Task2 artifact_manifest_sha256 alias was unexpectedly accepted`. The parser did
not output the second alias, so the shell comparison could not check it.

**Repair and GREEN:** `SC26-AE/lib/task2_echo.sh` now parses both aliases and requires both to equal
the verified manifest digest. `tests/integration/test_sc26_ae_task2_contract.sh` adds the
second-alias mutation case and restores both aliases before continuing. The final GREEN transcript
is `logs/task2-pointer-alias-green-session45-final.log` (exit/status `0`), and the affected
regression is `logs/task2-pointer-affected-session45.log`.

No acceptance threshold, checksum semantics, fallback, source-selection rule, or evidence class
was weakened. This closure does not qualify a real Task2 predictor.

### I51. Task1 AE producer source surface is not bound to the recorded outer commit

**STATUS: OPEN / BLOCK.** The Session 45 audit found the public Task1 wrappers, common helper,
Task1 runner, and artifact-manifest tool absent from the current outer `HEAD` while being executed
from an untracked working-tree overlay. A manifest containing only `git rev-parse HEAD` therefore
cannot reproduce the producer bytes. See F10-01 and the raw audit log.

Closure requires a tracked/snapshotted producer surface, pre/post byte checks, and execution from
the exact recorded snapshot. Merely editing a commit string in metadata is not a valid repair.

### I52. Task1 MoE QUICK scope has no machine-enforced full-rank promotion gate

**STATUS: OPEN / BLOCK.** Qwen/DSV3 QUICK can carry `profile=full` and `world_size=256` while
capturing only `0,64,128,192`; current sealer/packager checks do not require the exact `0..255`
set for release evidence. QUICK remains a valid synthetic smoke subset, but it must never promote
to full qualification. See F10-02.

### I53. Task1 semantic artifact and timing gates are incomplete

**STATUS: OPEN / HIGH/WATCH.** Task1 marker publication checks inventory and non-empty files but
not all target operations, timing/identity fields, DDP trigger metadata, or real SQLite/NVTX
semantics. Real `nsys` is PATH-selectable rather than bound to the documented canonical executable.
The plan's D16 fields are specified but the runner emits only `capture_elapsed_seconds`. See
F10-03 and F10-04. Closure requires an explicit semantic/timing contract and negative tests.

### I54. Task2 qualified reuse and pointer publication have no closed lifecycle

**STATUS: OPEN / BLOCK.** Real reuse requires `real_exact_two_h800_qualified`, but the subsequent
verifier and marker writer accept only raw/pending evidence. The sealer publishes a new sealed tree
and receipt but no canonical qualified shared pointer; changing the destination basename conflicts
with predictor-ID identity checks. See F10-05 and F10-06. This requires an approved state-machine
and identity design before code changes.

### I55. Task2 nested interpreter and producer provenance binding are incomplete

**STATUS: OPEN / HIGH/BLOCK.** The fixed outer Python launcher does not prove that Echo subordinate
modules use the same executable, and Task2 metadata records an outer `HEAD` that omits the actual
AE wrappers/tools. See F10-07 and F10-08. Closure requires a complete fixed-interpreter chain and
tracked producer snapshot.

### I56. Task2 self-consumer trusted paths and cross-file identity checks are weaker than packaging

**STATUS: OPEN / MEDIUM.** Lexical path checks permit intermediate symlink escape, and reuse does
not fully cross-check marker, manifest, provenance, execution mode/evidence, command identity, and
path aliases. See F10-09. Add a shared trusted-path/identity helper and negative tests before
qualified reuse.

### I57. Task3 resolver and package builder lack a single frozen input expectation snapshot

**STATUS: OPEN / HIGH.** Fresh root validation resolves paths before lexical containment checks and
later consumers can read live source paths after validation. Package summary fields and marker
identity checks also have known consistency gaps; generic manifests and prebaked traces are less
semantic than fresh inputs. See F10-10 and F10-11. Closure requires a design-approved immutable
snapshot seam and schema-specific verification.

### I58. External issuer authentication is still a release governance blocker

**STATUS: OPEN / BLOCK (CR-01).** The sealer verifies bytes and fields but does not authenticate an
issuer cryptographically. No local synthetic test can promote this state. Closure requires an
approved trusted-key/signature protocol and issuer material; do not invent a substitute in the
working tree. See F10-12 and the independent code-review CR-01 record.

## Session 45 bounded-repair disposition — 2026-07-19

The current working tree contains three narrow validator hardenings with fresh RED→GREEN evidence:

- Task1 now rejects missing `forward_step`, `backward_step`, or `optimizer_step`, incomplete
  backward metadata, and missing DDP trigger metadata before marker publication. This closes only
  the producer-side semantic precondition that was already required by the Task3 consumer; I53
  remains **OPEN / HIGH/WATCH** because SQLite/NVTX semantics, canonical `nsys` identity, D16
  timing fields, and complete producer provenance are still missing.
- The package consumer now requires path-free Task3 `simulation_run_id`, canonical
  `run_path`, and exact marker/manifest identity for `simulation_run_id`, `capture_id`, and
  `predictor_run_id`. This is local split-brain hardening only; I57 remains **OPEN / HIGH** because
  trusted-root containment, frozen input snapshots, and schema-wide semantic checks are unresolved.
- The package summary now iterates to a bounded fixed point and the verifier recomputes the final
  byte total and distribution medium. This fixes the observed `51,138` versus `51,207` byte
  discrepancy (absolute delta `69`) but does not define or approve the release package schema.

The fresh local matrix is recorded in
`test_report_2026-07-19_bounded_validator_repairs.md` and
`logs/session45-bounded-repairs-regression.log` (exit `0`, pytest `68 passed`, Task1 `21/21`,
Task3 contract `10/10`, portability `17/17`, shell syntax `73`, Python syntax `160`). These
results do not close I51, I52, I54, I55, I56, I58, or the remaining I53/I57 conditions, and they
do not promote any synthetic evidence to real qualification.

## I53 local memory-validator coverage addendum — 2026-07-19

**STATUS: LOCAL TEST-COVERAGE GAP CLOSED; RELEASE FINDING REMAINS OPEN.**

The Task1 integration fixture now exercises ten validator failure branches: empty payload, empty
samples, NaN peak, Infinity peak, zero peak, negative reserved memory, negative allocated memory,
all-zero samples, missing rank, and duplicate rank. Every case fails before marker publication and
asserts the specific production error. The first RED was fixture-only (`FAKE_MEMORY_MODE` was
ignored and valid JSON was always emitted); the minimal fixture repair produced `PASS_COUNT=31`
with exit `0`, followed by Task1 smoke `SMOKE_PASS_COUNT=1` and fresh-chain
`CHAIN_PASS_COUNT=1`.

This does not close I53/F10-03 or F10-04. Real SQLite/NVTX semantics, canonical `nsys` path/version
binding, D16 timing fields, and complete producer provenance still require design-approved or
real-runtime evidence. The evidence class is
`local_synthetic_not_gpu_qualification`; Gate B1 and release pre-dataset statuses are unchanged.

## Session 45 verifier v8 harness RED — 2026-07-19

**STATUS: VERIFIER-ONLY RED; CORRECTED RERUN PENDING.** The immutable v8 verifier stopped because
its expected `future.md` SHA256 literal transcribed `...d91ef74...` instead of the measured
`...d91fe74...`. Its outer status-capture wrapper also continued to print a PASS marker after the
inner failure. The repository file and its latest summary row were not changed by this failure.

The preserved RED log is
`logs/session45-final-verification-v8.log` (bytes `1,432`, SHA256
`5f6586e6df574d5771c0a05c6e999af96c434dc57b647a6a9a04dffecc2748b9`). The next verifier must use
the measured digest and fail-fast subshell semantics. This does not alter I51-I58, CR-01, Gate B1,
or any evidence class.

### Current regression evidence supersession — 2026-07-19

The first bounded-repair matrix reported Task1 `21/21`, but a subsequent test-only memory
validator lane expanded the current contract suite to `31` cases. The initial transcript remains
historical; the final current-working-tree evidence is
`logs/session45-bounded-repairs-regression-v2.log` (exit `0`, pytest `68 passed`, Task1 `31/31`,
Task3 `10/10`, portability `17/17`, shell `73`, Python `160`). This changes no issue disposition:
I53 remains open for release semantics, I57 remains open for frozen roots/snapshots/schema, and
Gate B1/real/release pre-datasets/AE-ready remain blocked or unqualified.

## Session 45 verifier v11 harness-only RED

**STATUS: VERIFIER-ONLY RED; NO PRODUCT DEFECT.** The fresh v11 verifier stopped at the
`notes.md` row because its expected SHA256 literal transposed two adjacent nibbles (`...eb6f...`
was written as `...be6f...`). The measured bytes and digest are unchanged. The preserved log is
`logs/session45-final-verification-v11.log` (bytes `1,332`, SHA256
`de5b31f43a82c4f86e891a9317054ca004cea42c187310dd2c9e797935933b70`, exit `1`).

The root cause is manual verifier input transcription plus the fact that the v11 predicate did
not derive its expectation from the authoritative current inventory table. The remediation is a
new fail-fast v12 parser that reads the uniquely marked summary inventory and compares every listed
file's current bytes and SHA256. No production code, acceptance threshold, provenance contract,
fallback, evidence class, or qualification state changed. Until v12 is independently read and
verified, this documentation checkpoint is pending; I51-I58 and CR-01 remain open.

### Session 45 I57 clean-clone follow-up and dependency boundary — 2026-07-19

**Motivation:** The first clean-clone replay returned non-zero even though all internal public cases
passed, and the current Task3 portability contract had just gained an intermediate Task1-root
symlink negative case. A current working-tree audit was required before final document hashing.

**Expectation:** Reject any intermediate Task1-root or Task1-runs symlink before fresh-input
resolution; keep the isolated replay count synchronized with the current Task1 contract; and
record any unrelated environment failure without masking it or changing release semantics.

**Method:** Kept the I57 production/portability repair and changed only the clean-clone assertion
from `PASS_COUNT=11` to `PASS_COUNT=31`. Re-ran the clean-clone replay, all four standalone e2e
paths, the full SC26-AE unit/integration/e2e matrix, broad shell/Python syntax, and diff checks.
Also ran the modified grouped-gemm setup/GPT mock tests and then attempted the grouped-gemm runtime
module exactly as written.

**Result:** Clean-clone GREEN and the full SC26-AE matrix are recorded with exit `0`; the current
portability count is `18`, not the historical `17`. The grouped-gemm runtime collection fails with
`ModuleNotFoundError: grouped_gemm` because the controller has no installed v1.0 module. This is an
environment prerequisite gap and was not converted into a fallback or a false PASS.

**Residual issue:** I57 is not closed. The repair does not freeze producer bytes, bind builder and
simulator to one immutable snapshot, or define the package schema. I53 SQLite/Nsight/D16 semantics,
I51 producer snapshot, I52 full-rank promotion, I54 lifecycle, I55 nested interpreter, I56 shared
trusted paths, and I58 issuer authentication remain open. Global status is unchanged:
`INCOMPLETE`; Gate B1 `BLOCKED`; real/release pre-datasets `NOT QUALIFIED`; `AE-ready=NO`.

## Session 46 I54/I56 behavioral confirmation — 2026-07-19

### I56 / F10-09: shared-pointer trusted-root check is too late

**STATUS: OPEN / MEDIUM-HIGH.** A retained synthetic probe placed a lexical-safe shared pointer
under `_shared/task2/runs/fresh-chain-synthetic-predictor` while making that final component a
symlink to a run outside the probe output root. The pointer ID and both manifest checksum aliases
were internally consistent. The current consumer entered canonical manifest verification and
reported `MANIFEST_STATUS=verified` with `MANIFEST_FILE_COUNT=13` before the later marker-writing
step raised `ValueError` from `Path.relative_to(output_root)`. The root boundary is therefore not
established before external bytes are read.

Probe report:
`/data/ycfeng/sc26-ae-test-tmp/session46-task2-symlink-probe-20260719/probe_report.md`
(1,970 bytes; SHA256
`0fe8e03b677e071d327f8f021140693a241273954ee2099200738db98cf9772f`).

**Root cause:** the consumer performs lexical checks and then resolves/reads the pointed-to run;
containment is checked only downstream. **Required design:** reject lexical symlink components and
prove exact canonical `_shared/task2/runs/<predictor_run_id>` containment before opening any bundle
file. No implementation was applied in this continuation.

### I54 / F10-05: qualified evidence state is internally contradictory

**STATUS: OPEN / BLOCK.** A second probe supplied
`execution_evidence=real_exact_two_h800_qualified`. The exact production reuse precheck returned
`REUSE_VALIDATOR_RC=0`, while the canonical evidence predicate embedded in `task2_verify_run`
returned `VERIFY_EVIDENCE_PREDICATE_RC=1` with
`Task2 artifact manifest execution evidence is invalid`.

Probe report:
`/data/ycfeng/sc26-ae-test-tmp/session46-task2-qualified-lifecycle-probe-20260719/probe_report.md`
(1,300 bytes; SHA256
`fd47157747bc5d19a4886a82d7c76e974b645340187aa090c9df3cbd0dcd7255`).

**Root cause:** reuse and canonical verification implement different evidence sets. **Required
design:** one approved state machine for raw, pending-external, and qualified states, shared by
manifest verification, reuse, marker publication, sealer output, and shared-pointer publication.
Do not widen one conditional in isolation.

These probes confirm findings; they do not qualify a GPU run or authorize a release promotion.

## Session 46 D30 Task2 canonical-containment repair — 2026-07-19

### I56 / F10-09 partial resolution

**STATUS: PARTIAL / OPEN.** The test-exposed intermediate-symlink escape was repaired in the two
Task2 reuse resolvers. The model-marker and shared-pointer paths now canonicalize the selected run
and require it to be relative to the canonical `AE_OUTPUT_ROOT` before opening `artifact_manifest`,
`metrics`, or `provenance` files.

**Motivation:** The prior lexical checks accepted `_shared/task2/runs/<safe-name>` when an
intermediate component was a symlink to an external directory. The retained probe showed the
consumer could read 13 external artifact files before a later `relative_to` operation failed.

**Expectation:** A fixture with internally consistent external metadata must be accepted by the old
implementation (valid RED), then rejected by both resolvers after the guard, without changing any
schema, evidence class, or fallback behavior.

**Method:** The integration fixture now builds two complete synthetic runs outside the trusted root,
with matching predictor IDs and checksums, then uses lexical-safe intermediate symlinks. The
production patch adds only `Path.resolve()` plus `relative_to(output_root)` guards in the two
embedded Python resolvers. Targeted and full regressions were rerun.

**Result:** RED exit `1` is recorded in
`logs/task2-containment-fixture-red-20260719.log`; the old code explicitly reached the acceptance
branch. GREEN exit `0` is recorded in `logs/task2-containment-green-20260719.log`; both symlink
cases fail with their dedicated containment errors and valid reuse remains green. The full local
rerun passed `73` Python tests, Task1 `31`, Task3 contract `10`, portability `18`, grouped-gemm
setup `37`, GPT mock `22`, and all e2e/clean-clone checks.

**Residual boundary:** This does **not** close full I56. It does not add a shared trusted-path
helper, prove a broader exact canonical run shape, solve TOCTOU/frozen-input semantics, or add
cross-file marker/manifest/provenance equality. It also does not address I54's contradictory
qualified-evidence lifecycle, I55 nested interpreter binding, I57 snapshot seam, or I58 issuer
authentication. The independent review approved canonical containment only and blocked evidence
lifecycle/equality changes. Do not widen accepted evidence or relabel synthetic output.

### Harness-only failures resolved during this change

1. The first full-regression wrapper omitted `mkdir -p` for the per-case `gpt-mock` temp parent;
   `mktemp` failed before the GPT mock test. The wrapper was corrected and the entire matrix was
   rerun successfully; no repository code caused this failure.
2. The first static wrapper scanned intentional negative-test literals (`/tmp/other-python`) as
   forbidden production temp paths. The scope was corrected to `SC26-AE/lib` and `SC26-AE/tools`;
   all 73 shell and 201 Python files still compiled cleanly and the production scan passed.
3. One initial command used an incorrect worktree path and therefore found no project files; it
   produced no repository mutation. The corrected command generated the authoritative static log.

Global disposition remains `INCOMPLETE`; Gate B1 is `BLOCKED`; real/release pre-datasets are
`NOT QUALIFIED`; `AE-ready=NO`.

## Post-implementation review WATCH items — 2026-07-19

**Status:** `I56 PARTIAL / OPEN`; this section records residual risk after the approved narrow
intermediate-symlink containment repair. It is not a new production defect and does not authorize
an I54/I56 architecture change.

### WATCH-01 — resolved containment is not a complete trusted-root contract

Both resolver snippets call `Path(...).resolve()` on `AE_OUTPUT_ROOT` and then apply
`run_root.relative_to(output_root)`. This correctly rejects the tested stable external
intermediate symlink, but it does not prove that the lexical output root itself, or each parent
component, is non-symlink. It also does not require the exact canonical run shape
`_shared/task2/runs/<predictor_run_id>` or equality of `run_path` and `run_relative_path`.

**Root cause:** the approved patch was intentionally limited to post-resolution containment; no
shared trusted-path seam or identity state machine was approved for this pass.

**Required future resolution:** obtain owner approval for one trusted-path design, then add its
lexical-root, exact-run-identity, and alias-equality checks with RED→GREEN coverage. Do not add
another isolated inline condition in this task.

### WATCH-02 — pathname check/use TOCTOU remains open

After the containment check, the resolvers still read `artifact_manifest.json`, `metrics.json`,
and `provenance.json` through ordinary pathnames. A concurrent replacement of a writable parent
directory could therefore change the object between check and use. The current guard is not a
descriptor-anchored traversal or immutable input snapshot.

**Root cause:** frozen-input/snapshot semantics belong to the open I57/trusted-path design and
were explicitly outside the approved narrow repair.

**Required future resolution:** design and review a descriptor-anchored or immutable-snapshot
seam, then test the race/alias contract without weakening existing evidence checks.

### WATCH-03 — RED evidence scope is asymmetric

The retained containment RED log directly proves only the old model-marker acceptance branch. The
post-fix GREEN log proves both model-marker and shared-pointer rejection. The older Session 46
shared-pointer symlink probe is supporting pre-fix evidence, but should not be described as an
independent RED transcript for the new fixture. This is an audit/evidence precision issue, not a
reason to change production behavior.

## Session 46 primary-agent independent containment recheck — 2026-07-19

**STATUS: VERIFIED NARROW SEAM / I56 PARTIAL / OPEN.** The integrating agent independently reran
the current Task2 integration and smoke contracts, fresh chain, clean-clone-style replay, and the
full local matrix after the D30 repair. The results confirm the tested post-resolution containment
guard but do not resolve the broader I56 trusted-path, identity, or snapshot contract.

### Evidence

| Check | Exit | Bytes | SHA256 |
|---|---:|---:|---|
| Task2 integration | `0` | `1086` | `7b2bbea97c816110d050067a754033b915bf9f150f5eda84fcecaecb19eb718d` |
| Task2 smoke | `0` | `1145` | `7dc98413038c9ab89b8f4656f5d0fd6eb2691cc5e6f7d8311e27f13be358c358` |
| Fresh chain | `0` | `789` | `3b230e159ea0c34d21f1bbf943dcf72e69c715e9e101edf1dbdcc33ef6ae815c` |
| Clean-clone replay | `0` | `1408` | `0e64fa6439a1380808304a91507affd477928c29a017c87c5335a354bffcabc0` |
| Full matrix/static | `0` | `23476` | `a01816d3416045865c2476e3ddf6c2bb4c785068d72f2f69317a0ac69d680d8e` |

The full run observed `73` Python tests, Task1 `31`, Task3 contract `10`, portability `18`, clean
clone public entries `3/3/3`, setup cases `6`, fresh chain `1`, Task3 models `3/3`, grouped-gemm
setup `37/37`, GPT mock `22/22`, shell syntax `73/73`, Python compile `201/201`, a passing
production temp-root scan, and `git diff --check`. Fresh-chain numeric values were trace/memory
`4/4`, validation/test MSE `3.0/0.5`, reload delta `0.0`, rank0 step `22.5 ms`, forward/backward/
optimizer `6.0/11.0/2.5 ms`, simulator wall `0.5 s`, and peak RSS `51,336 KiB`; all are synthetic
controller values.

### Root-cause and residual boundary

The current guard correctly rejects the tested stable intermediate-symlink escape before attaching
the model marker. It still does not provide lexical no-symlink root validation, exact canonical
`_shared/task2/runs/<predictor_run_id>` shape, `run_path`/`run_relative_path` equality,
cross-file marker/manifest/provenance equality, descriptor-anchored TOCTOU protection, or a frozen
input snapshot. I54, I55, I57, and I58 remain open; no evidence class or release state changed.

The controller remains `local_synthetic_not_gpu_qualification`; global state remains
`INCOMPLETE`, Gate B1 `BLOCKED`, `real_pre_dataset=NOT QUALIFIED`,
`release_pre_dataset=NOT QUALIFIED`, and `AE-ready=NO`.

## V20 verifier-only harness RED — 2026-07-19

**STATUS: HARNESS RED RESOLVED; NO PRODUCT OR QUALIFICATION IMPACT.** The first V20 verifier
wrapper printed a supplemental digest mismatch but returned zero because the Python checker was not
under shell fail-fast control. Its expected full-regression digest also described the log before
the log appended its own summary lines. A strict rerun correctly returned non-zero when the summary
did not yet contain an explicit `I56 PARTIAL / OPEN` marker.

### Root cause and evidence

- `logs/session46-final-verification-v20.log`: bytes=`4704`, SHA256
  `16a8d539f2f324861eac1836c3cec87271dbc5653383e016cc404ba41d9eab9a`; semantic mismatch was
  printed, but the wrapper ended with a false `VERIFIER_EXIT=0`.
- `logs/session46-final-verification-v20-rerun.log`: bytes=`4173`, SHA256
  `85c710c085c43ef7e43919316805903c9a64603f486e33479b7d1aa70f9cd047`; strict fail-fast exit=`1`
  on the missing status marker.
- The full-regression log's final identity is bytes=`23476`, SHA256
  `a01816d3416045865c2476e3ddf6c2bb4c785068d72f2f69317a0ac69d680d8e`, after its summary lines;
  the earlier pre-append identity is not used.

### Resolution and residual risk

The verifier command was corrected with `set -euo pipefail`, explicit return-code handling, and
final-byte digest measurement; the summary now states `I56 = PARTIAL / OPEN`. No acceptance
threshold, evidence predicate, production path, fallback, or release label was changed. A final
V21 inventory/verifier pass is required after the resulting documentation-byte changes. I51–I58
and CR-01 remain open, and the global status remains `INCOMPLETE` with Gate B1 blocked.

## Session 47 V21 verifier harness — documentation-only correction

**Status:** OPEN until the final post-append V21 run is independently observed; no product or
qualification impact.

**Root cause:** The uniquely marked V21 artifact inventory lagged the final I52 worktree by four
rows. The previous V21 attempt also used a stale expected identity for a historical V20 log. A
verifier that did not parse the unique markers and propagate child-process failures would permit a
false GREEN.

**Resolution:** Added the strict shell/Python verifier pair
`tests/integration/test_sc26_ae_v21_verifier.sh` and
`tests/integration/sc26_ae_v21_verifier.py`, corrected only the V21 current rows, and preserved
all historical inventories and RED logs. The checker rejects duplicate/malformed inventory rows,
recomputes bytes/SHA256, resolves supplemental identities, checks I50-I58, validates the blocked
status boundary, runs the documentation contract, compiles the established static scope, scans
production roots for hard-coded temporary paths, and runs `git diff --check`. A deliberate
`--expected-status PASS` run while the summary still said `VERIFIER_PENDING` returned exit `1`,
confirming fail-fast propagation.

**Residual boundary:** This verifier is local control-plane evidence only. It does not qualify an
H800 run, grouped-gemm runtime, issuer, release package, trusted-root lifecycle, or `AE-ready`
state. I51, I54, I55, I56, I57, and I58 remain open or partial; `INCOMPLETE`, Gate B1
`BLOCKED`, `real_pre_dataset`/`release_pre_dataset` `NOT QUALIFIED`, and `AE-ready=NO` remain
unchanged.

## Session 47 V21 post-append verifier result

**Status:** RESOLVED for the local documentation/static harness; all release and hardware issues
remain open.

**Motivation:** The initial V21 status was intentionally pending until the verifier log identity
could be measured. A second run was needed after appending that identity so the checker could catch
any self-reference or stale digest.

**Expectation:** The recorded verifier log must match its measured bytes/SHA256, and every prior
inventory/status/static assertion must remain true after the append.

**Method and result:** The post-append command exited `0` and validated the final log identity
(`6150` bytes, SHA256=`f8480ee9178bd2bbc56c528a42b7c02658efdb77949446104fddd6157594b17a`),
artifact/document rows=`7/10`, supplemental rows=`17`, issue headings `I50..I58`, docs=`9/10`,
shell/Python=`52/35`, temporary-root scan=`PASS`, and `git diff --check=PASS`. The deliberate
wrong-status RED remains retained as harness evidence and is not a product failure.

**Residual boundary:** The checker closes only the local V21 control-plane checkpoint. It does not
close I51, I54, I55, I56, I57, or I58, does not qualify H800/grouped-gemm execution, and does not
promote either pre-dataset or `AE-ready`. `INCOMPLETE`, Gate B1 `BLOCKED`, both pre-datasets
`NOT QUALIFIED`, and `AE-ready=NO` remain authoritative.

## Session 48 I54 canonical evidence predicate repair — 2026-07-20

**Status: PARTIAL / OPEN (local contradiction repaired; lifecycle still blocked).**

### Root cause

The mode-specific Task2 reuse validator accepted `real_exact_two_h800_qualified`, but the
canonical evidence allowlist inside `task2_verify_run()` omitted it. The same manifest could pass
the reuse precheck and then be rejected by canonical verification with
`Task2 artifact manifest execution evidence is invalid`. This was an internal predicate mismatch,
not a missing hardware qualification.

### Resolution

The minimal change in `SC26-AE/lib/task2_echo.sh` adds
`real_exact_two_h800_qualified` to the canonical terminal evidence set. It does not alter the
real-mode reuse requirement, synthetic-mode allowlist, pending-evidence handling, thresholds,
source selection, marker/pointer publication, or evidence labels. The RED/GREEN transcripts are
`logs/i54-qualified-verify-red-20260720.log` (exit `1`) and
`logs/i54-qualified-verify-green-20260720.log` (exit `0`).

### Verification

The affected Task2 regression exited `0`: evidence-mode unit `PASS_COUNT=4`, interpreter contract
`PASS_COUNT=11`, snapshot unit exit `0`, and Task2 integration exit `0`. The qualified fixture's
manifest had `MANIFEST_STATUS=verified` and `MANIFEST_FILE_COUNT=13`; canonical verification
changed from `QUALIFIED_VERIFY_STATUS=rejected` to `QUALIFIED_VERIFY_STATUS=accepted`.

### Residual boundary

This repair does not define the qualified shared-pointer lifecycle, destination identity, marker /
manifest / provenance cross-file equality, or real external attestation. I54 remains `PARTIAL /
OPEN`; Gate B1, both pre-datasets, and `AE-ready` remain blocked/unqualified.

## Session 48 I55 nested interpreter audit — 2026-07-20

**Status: OPEN / HIGH/BLOCK; read-only RED confirmation.** A synthetic CPU-only probe showed that
the fixed outer launcher does not yet bind subordinate Echo modules to the same executable. Four
generated configs carried an external `python_path`; the subordinate path made one external
invocation and created an external sentinel (`EXTERNAL_INVOCATIONS=1`, `NESTED_SENTINEL=created`)
before the mocked metrics stage failed (`PROBE_RC=1`). The probe used the ephemeral fixture
`/tmp/sc26-i55-red3.VpcQhY` and produced no durable repository artifact. It therefore remains an
audit finding, not a qualification or release result.

Closure still requires an approved fixed-interpreter chain, canonical executable identity/hash
binding, and producer provenance coverage for all nested modules. Do not patch pinned Echo or
silently rewrite config paths as a fallback.

### I55 durable reproduction and disposition — 2026-07-20

**Motivation:** The first I55 probe was ephemeral. A repeatable repository-side audit record was
needed before deciding whether any runtime-chain change could be proposed.

**Expectation:** A fixture that mirrors the current outer call (`fixed_python update_configs.py`
followed by `bash run_all.sh`) must show whether PATH-dependent nested configuration can escape the
fixed interpreter, without changing pinned Echo or using a host fallback.

**Method:** A CPU-only fixture was created under a writable temporary root and invoked the existing
`task2_run_build` library with producer/manifest writers stubbed only at the fixture boundary. The
fixture's `update_configs.py` used the same `which python` behavior as pinned Echo, generated all
four canonical config paths, and its nested module executed the configured path. The full command
transcript is stored at
`logs/i55-nested-interpreter-red-20260720.log` (1,431 bytes,
SHA256=`0551da75f50e9801e875ba55ab036ce31b42c54d187c2239766da2050ac551f4`).

**Result:** The current wrapper returned `PROBE_RC=1` at the later mocked prediction boundary, but
the nested chain had already escaped: `EXTERNAL_INVOCATIONS=1`, `NESTED_SENTINEL=created`, and all
four configs contained the external executable path. The result is strictly
`local_synthetic_not_two_gpu_qualification`; it is not a real Echo run, qualification, or release
artifact.

**Disposition:** Keep I55 `OPEN / HIGH/BLOCK`. The independent read-only review by
`/root/audit_i54_i55` supports a wrapper-only candidate consisting of PATH binding, canonical
path/hash checks, post-update validation of all four configs, and a manifest-bound sidecar. The
current setup contract does not provide an approved interpreter digest, so the sidecar schema and
authority source remain owner-approved design inputs. No production code was changed in this
checkpoint; no evidence class, threshold, source selection, or release state changed.

## Session 48 V21 inventory refresh precheck — 2026-07-20

**Status: RESOLVED AS DOCUMENTATION-HARNESS INPUT.** The first V21 run after the I54 report and
Task2 regression append correctly rejected the stale non-self-referential inventory before any
later checks. The documentation contract was already `PASS` with public entries=`9` and paper
suggestions=`10`; the strict checker reported progress expected bytes=`222799` versus actual
bytes=`227866` and returned exit `1`. The preserved transcript is
`logs/session48-v21-stale-inventory-red-20260720.log` with the propagated marker
`SESSION48_V21_STALE_INVENTORY_RC=1`.

The root cause is ordinary append-only documentation changing a listed file without refreshing
its recorded digest. The fix is to recompute the current V21 rows and rerun the checker. No
acceptance threshold, evidence class, source path, or qualification state is changed.


## I55 — Nested interpreter chain semantic binding — local checkpoint, still OPEN / HIGH / BLOCK

**Status:** Local wrapper/test semantic contract is GREEN; I55 closure is **not** approved.

### Root cause

The outer Task2 wrapper used an absolute fixed interpreter, but pinned
`Echo-slowdown/update_configs.py` resolved `python` through `which`. The four generated configs
could therefore carry an unrelated executable into subordinate `run.sh` scripts, which use both
bare `python` and `${python_path}`. A caller-side string equality check did not cover that nested
chain.

### Local resolution and evidence

The wrapper now binds the fixed directory at the front of `PATH`, checks requested/canonical path
and observed SHA256, validates all four live configs before and after the nested run, archives their
post-update bytes, and records a strict `interpreter_binding.json` sidecar referenced by provenance
and the generic manifest. Semantic verification is independent of generic checksum verification:

- duplicate JSON keys are rejected instead of silently taking the last value;
- artifact-only verification rejects lexical non-canonical canonical paths;
- synthetic callers inspect real-evidence sidecars while synthetic bundles remain sidecar-free;
- a missing sidecar-validator argument fails with a controlled diagnostic;
- coherent tamper cannot publish a model marker or mutate the shared pointer.

The final regression is `logs/i55-final-regression-20260720.log` (8,613 bytes,
SHA256=`b6859ea9508328c664ec81e6906051759c97b2b3dae9d33fd958a49c87b6bd2b`, exit `0`). Its key
counts are parser negatives=`11`, duplicate-key negatives=`4`, sidecar tamper negatives=`9`,
interpreter PASS=`12`, evidence-mode PASS=`5`, and artifact/sealer/package pytest=`94 passed`.
The formal report is
`test_report_2026-07-20_i55_interpreter_chain.md`.

### Residual blockers

The local executable hash is an observed fixture value, not an authority-approved immutable image
digest. The canonical v1.2-ae worker, external issuer, producer source snapshot, frozen inputs,
trusted-root/TOCTOU design, and Nsight tool binding are not established here. Generic
`artifact_manifest.py` JSON readers outside the exercised I55 semantic paths still have potential
duplicate-key exposure and are a separate hardening candidate requiring its own RED test.

### Disposition

Keep I55 `OPEN / HIGH / BLOCK`, Gate B1 `BLOCKED`, `real_pre_dataset` and `release_pre_dataset`
`NOT QUALIFIED`, and `AE-ready=NO`. Do not describe the synthetic fixture's sidecar hash as an
approved digest or use this checkpoint to close I51/I53/I54/I56/I57/I58/CR-01.

## Session 51 I53/D16 model-aware timing contract — 2026-07-20

**Status: OPEN / HIGH/WATCH.** This checkpoint repairs the local metadata semantics for the
already-observed Task1 rank timing. It does not establish an independent D16 preflight, real H800
timing, SQLite/NVTX qualification, or release evidence.

### Root cause

The first D16 writer treated every model as if the frozen MoE rule applied: `rank0 timing × 256`
with a `7200`-second fresh-capture gate. GPT-175B, however, captures eight PP-stage representative
ranks (`capture_scope=representative`), so `256` described neither the selected capture nor a
meaningful GPT estimate. The resulting fields were numerically consistent but semantically
misleading. A second boundary was also exposed: the measured rank-0 interval is collected while
the selected-rank capture is already running, so it cannot decide whether that capture should have
started.

### Resolution

The smallest producer/validator change in `SC26-AE/lib/task1_trace.sh` now has an explicit model
contract:

| Model | `d16_gate_applicable` | Estimate count | Gate fields |
|-------|----------------------:|---------------:|-------------|
| GPT-175B | `false` | `8` representative ranks | omitted |
| Qwen3-A30B | `true` | `256` ranks | threshold `7200`, result `pass\|prebaked_required` |
| DeepSeek-V3 | `true` | `256` ranks | threshold `7200`, result `pass\|prebaked_required` |

The validator fails closed on unsupported models, non-boolean applicability, model/count mismatch,
GPT gate fields, missing or inconsistent MoE gate fields, non-positive timing, rank inventory
mismatch, and summary/metadata disagreement. For every model the estimate is checked exactly as:

```text
estimated_full_seconds = single_rank_elapsed_seconds × estimate_rank_count
```

No threshold, evidence class, source-selection rule, automatic fallback, marker publication rule,
or release status was changed.

### Verification evidence

The durable RED/GREEN transcript and the fresh regression log are:

| Evidence | Result | Bytes | SHA256 |
|----------|--------|------:|--------|
| `logs/i53-d16-model-aware-red-20260720.log` | RED, exit `1`; old validator required a GPT-forbidden gate field | 129 | `cc753674701a70b68a1de453e40ef6808884580fdd6cc3d26c1365556975ccea` |
| `logs/i53-d16-model-aware-green-unit-20260720.log` | GREEN, `PASS_COUNT=29` | 1,256 | `8d412b844d392055e40bb5292b23915951c48d48f4c16722eed6ae5143052e13` |
| `logs/i53-d16-model-aware-green-integration-20260720.log` | GREEN, `PASS_COUNT=35` | 2,381 | `2a32a75a52a9782ad45317e949ea7ca2ac0139861e444bc1df641bb473803594` |
| `logs/i53-d16-regression-matrix-20260720.log` | Source provenance `2`, D16 `29`, Task1 `35`, Task3 `10`, pytest `94` | 5,212 | `730e0d6df02318b04c0fea900411e7bd4be932231b4ad0932a74c16f17df3d51` |
| `logs/i53-final-local-verification-20260720.log` | Static checks, affected suites, clean clone, and pytest all exit `0` | 10,193 | `b94f3c9d61f13641e7564ad2c27ec122d11d8d02fcb09ebb5b26970f2245b586` |

The current fresh synthetic fixture observed these arithmetic values:

| Model | Rank-0 elapsed (s) | Estimate count | Estimated full seconds | D16 result |
|-------|-------------------:|---------------:|-----------------------:|------------|
| GPT-175B | `0.009844431` | `8` | `0.078755448` | not applicable |
| Qwen3-A30B | `0.013280299` | `256` | `3.399756544` | `pass` |
| DeepSeek-V3 | `0.010431187` | `256` | `2.670383872` | `pass` |

These values are local synthetic arithmetic observations only. In particular, the MoE `pass`
values do not mean that a full fresh capture completed within 7200 seconds, because the timing was
observed inside the selected capture rather than from an independent rank-0-only probe.

### Remaining boundary and disposition

Closure still requires a separately implemented rank-0-only probe that runs before full capture,
real H800 timing, canonical `nsys`/SQLite/NVTX semantics, producer source snapshot/provenance, and
the independent I51/I54/I55/I56/I57/I58/CR-01 controls. Keep:

```text
I53 = OPEN / HIGH / WATCH
Gate B1 = BLOCKED
real_pre_dataset = NOT QUALIFIED
release_pre_dataset = NOT QUALIFIED
AE-ready = NO
```

Do not interpret `fresh_capture_gate_result=pass` from this synthetic selected-capture path as an
automatic source switch, fallback, full-capture approval, or release qualification.


## Session 51 V21 shell-scope drift and resolution — 2026-07-20

**Status: RESOLVED AS CURRENT-HARNESS ALIGNMENT.** This checkpoint repairs only the local V21
verifier's distinction between retained historical evidence and the current live worktree. It does
not close I53, qualify a worker, or promote any release source.

### Root cause

The retained Session 47 static transcript truthfully records the then-current scope:
`SHELL_SCOPE_COUNT=52` and `SHELL_SYNTAX_COUNT=52`. The current worktree contains the new
model-aware D16 unit contract, so live enumeration now finds `53` shell files (the verifier shell
itself remains excluded). The verifier had one hard-coded `52` in `verify_static_scope()`, which
caused a legitimate current-tree check to fail. A first correction changed the historical-marker
expectation as well; that produced a second RED because the archived transcript must remain `52`.

### Durable RED evidence

| Attempt | Result | Evidence |
|---|---:|---|
| Live scope against stale expectation | exit `1` | `logs/i53-v21-shell-scope-red-20260720.log`, `9482` bytes, SHA256 `51d84029119161b276e6e1b3252e8d3f89cb524c8c58e64e217ed53332e441cf`; error `shell scope count=53, expected 52` |
| Historical-marker drift attempt | exit `1` | `logs/i53-v21-shell-scope-green-20260720.log`, `9176` bytes, SHA256 `dd651767b6c8930ad5d94df509b28728acdcd417a2cd6e91bc512bb8a318fcf5`; error `missing static evidence marker: SHELL_SCOPE_COUNT=53` |

The second file name contains `green` for continuity with the attempted command, but it is a RED
attempt and must not be cited as passing evidence.

### Resolution and verification

- `verify_static_scope()` now requires and prints live `SHELL_SCOPE_COUNT=53` and
  `SHELL_SYNTAX_COUNT=53`.
- `verify_log_markers()` continues to require the historical transcript markers
  `SHELL_SCOPE_COUNT=52` and `SHELL_SYNTAX_COUNT=52`; historical evidence was not rewritten.
- `python3 -m py_compile tests/integration/sc26_ae_v21_verifier.py` returned `0`.
- `git diff --check` returned `0`.
- Direct Python V21 verification returned `0`; the transcript is
  `logs/i53-v21-verifier-green-20260720.log` (`9531` bytes, SHA256
  `33113ad76df4b220ec9af8c023b3946b2c64278f4adf6b356a4e33e96168425f`).
- The required shell entry point returned `0`; the transcript is
  `logs/i53-v21-shell-verifier-pass-20260720.log` (`9531` bytes, SHA256
  `f95275a5526a97cab931d64b6f6da3f6b5529c30e70f24f1ac3ec2901c48c3f2`). It reported artifact /
  authoritative rows `7/10`, supplemental identities `40`, issue headings `I50..I58`, live
  shell/Python scope `53/35`, `TMP_ROOT_SCAN=PASS`, `GIT_DIFF_CHECK=PASS`, and
  `SESSION47_FINAL_VERIFICATION_V21=PASS`.

The shell verifier's `DOC_CONTRACT_STATUS=PASS`, `PUBLIC_ENTRY_COUNT=9`,
`PAPER_SUGGESTION_COUNT=10`, and synthetic metrics remain unchanged. The V21 status is a local
harness status only; it does not alter the product evidence class or any qualification marker.

### Boundary

No GPU, RJob, Docker, H800, real SQLite/NVTX, independent rank-0-only preflight, or release
pre-dataset was run. Keep `I53=OPEN / HIGH/WATCH`, `Gate B1=BLOCKED`,
`real_pre_dataset=NOT QUALIFIED`, `release_pre_dataset=NOT QUALIFIED`, and `AE-ready=NO`.

### V21 post-documentation stale-inventory RED — 2026-07-20

After the scope correction was documented, a fresh shell verifier was intentionally run before
rebuilding the non-self-referential inventory. It failed closed at `progress.md` (expected
`241505` bytes, actual `244249`), exit `1`; the preserved transcript is
`logs/i53-v21-post-doc-stale-inventory-red-20260720.log` (`1420` bytes,
SHA256 `be966073732b7ea6cc394c6478859d69a8de61a02673740b1d33af96e7999696`). This is the
expected documentation-harness precondition, not a product or qualification failure. The inventory
must be refreshed only after all authoritative task documents stop changing.

### Independent review reconciliation for I53/D16 and V21 — 2026-07-20

`/root/audit_i54_i55` performed a read-only review. Its interim verdict was `APPROVE` for the
model-aware D16 metadata contract and the historical-52/current-53 separation, but `BLOCK` for
the then-current V21 snapshot because concurrent document appends had produced a duplicate
`V21_STATUS` substring and stale authoritative hashes. The review independently confirmed GPT
multiplier `8`, MoE multiplier `256`, threshold `7200`, GPT exclusion of MoE-only gate fields, and
that the current rank-0 timing remains selected-capture observation rather than an independent
preflight.

The duplicate marker was removed, all authoritative rows were refreshed, and a fresh candidate
verifier returned exit `0` with rows `7/10`, supplemental identities `43`, and live shell/Python
scope `53/35`. The review's `BLOCK` therefore records the pre-remediation snapshot, not the
corrected final V21 result. Its qualification warning remains binding: I53 is still `OPEN`, and
no independent rank-0 preflight or H800 qualification has been established.

## I53/D16 current-count drift and resolution — 2026-07-20

### Symptom

The first post-edit integration run completed all new behavior-matrix cases but failed with:

```text
FAIL: one warmup flag per invocation: expected '18', got '281'
```

The unit run had already reached `PASS_COUNT=49`; the stale integration assertion was the only
failure. A related clean-clone replay check still searched for the old `PASS_COUNT=35`, and the V21
verifier only checked the retained historical `PASS_COUNT=31` transcript.

### Root cause

The independent rank-0 preflight adds one controller invocation for each attempted capture, and the
full-pass matrix adds `1 + 256` calls before/through the selected loop. The test's total invocation
expectation was updated to `281`, but two per-invocation flag assertions and downstream replay
consumers were not updated together. The V21 check also conflated historical evidence with current
contract evidence.

### Resolution and verification

- Changed warmup/profile flag counts to `281`, matching the actual fake `torchrun` log.
- Changed clean-clone replay's current Task1 expectation to `PASS_COUNT=38`.
- Kept the historical Session 47 `PASS_COUNT=31` assertion and added separate current checks for
  `i53-d16-unit-green-20260720.log` (`PASS_COUNT=49`) and
  `i53-d16-integration-green-20260720.log` (`PASS_COUNT=38`).
- Fresh results: unit `49/49`, integration `38/38`, fake calls `281`, fresh-chain `1/1`, and smoke
  `1/1`, all exit `0`.

This issue is resolved for the local controller. It does not close I53 or any real qualification
boundary.

## Session 55 I55 synthetic real-bundle fixed-path binding — 2026-07-20

**Status: OPEN / HIGH / BLOCK.** The requested-path semantic gap is repaired for the local
wrapper-owned contract, but the complete I55 authenticity and authority handoff remains blocked.

### Motivation

The independent I55 review found that a synthetic caller could accept a checksum-coherent
real-evidence bundle whose `fixed_requested_path` named a different interpreter environment than
the wrapper's fixed `/opt/conda/envs/echo_slowdown/bin/python` literal. That made the fixed
interpreter contract depend on caller mode and left a real semantic escape in the artifact-only
verification path.

### Root cause

`task2_validate_binding_sidecar()` checked the sidecar's internal equalities and, for live callers,
checked the worker executable. In the synthetic branch `live_required=0`, however, it did not bind
the sidecar's requested path to the wrapper constant. An attacker could therefore rewrite the
requested path, canonical path, executable digest, archived configs, provenance, and generic
manifest checksums coherently and still pass the synthetic semantic consumer.

### Expectation

Every pending or qualified real-evidence bundle must satisfy
`sidecar.fixed_requested_path == TASK2_FIXED_PYTHON` before caller-mode branching. The synthetic
consumer must not need the `/opt/conda` executable to enforce that contract; live callers must
retain their existing canonical-path, regular-executable, and current-byte SHA256 checks. No
fallback, source switching, digest substitution, threshold change, or qualification promotion is
allowed.

### Method

1. Added a sidecar-focused RED→GREEN unit fixture with a temporary regular executable and
   synchronized sidecar, four archived configs, provenance, and listed manifest fields.
2. Added a complete generic-manifest integration fixture that also lists the alternate executable,
   then independently runs the generic manifest verifier and the semantic sidecar validator.
3. Confirmed the pre-fix acceptance in
   `logs/i55-alternate-fixed-path-red-20260720.log` (179 bytes,
   SHA256 `523bc4512efa12b1ba5f89a3d82f259a13e965b82a1b80ab3040ac28c4deaf8e`).
4. Added one common wrapper-literal comparison before the `live_required` branch, tightened nested
   test assertion propagation, corrected the synthetic integration positive fixture, and reran
   the affected matrix without touching pinned Echo.

### Result

The focused unit negative now rejects the alternate bundle in
`logs/i55-alternate-fixed-path-green-20260720.log` (3,103 bytes,
SHA256 `4b1f2a6453d603fb4e455a1e1b89e8874d6e44af6240771e8cac23ababbaf37f`) with
`ALTERNATE_FIXED_PATH_NEGATIVE=1`. The complete integration fixture independently reports
`MANIFEST_STATUS=verified`, `MANIFEST_FILE_COUNT=19`, and then rejects the same semantic rewrite;
its transcript is `logs/i55-qualified-alternate-path-integration-green-20260720.log` (1,272 bytes,
SHA256 `cfda836abc419db5fa7a3bffe4eea6c861bdb0065f4810a6c790d33b715c6a37`). The affected
regression returned exit `0` with `PASS_COUNT=12`, parser negatives=`11`, duplicate-key
negatives=`4`, sidecar tamper negatives=`9`, evidence-mode `PASS_COUNT=5`, and `94 passed in
5.13 s` for the artifact/sealer/package pytest subset.

### Residual blockers

The independent reviewer also confirmed that `live_required=0` cannot authenticate a bundle's
self-declared canonical path and SHA256 against an authority-approved worker identity. The local
canonical/hash-only coherent substitution probe remains accepted by the synthetic artifact-only
consumer and is retained as a known authority blocker, not hidden by this patch. Approved
immutable interpreter digest, canonical worker evidence, producer source snapshot, frozen inputs,
trusted-root/TOCTOU semantics, and downstream marker/pointer controls remain unresolved.

### Disposition

The initial independent verdict for the unpatched gap was `REQUEST CHANGES`. After the minimal
requested-path repair, the independent wrapper-only verdict is `COMMENT` (no new blocking code
finding), while the full I55 closure/release verdict remains `REQUEST CHANGES / BLOCK`. Keep
`I55 = OPEN / HIGH / BLOCK`, `Gate B1 = BLOCKED`, `real_pre_dataset = NOT QUALIFIED`,
`release_pre_dataset = NOT QUALIFIED`, and `AE-ready = NO`. This checkpoint does not close I51,
I53, I54, I56, I57, I58, or CR-01.

## Session 55 final-v2 evidence reconciliation — 2026-07-20

**Status:** I55 remains `OPEN / HIGH / BLOCK`; this entry records local evidence only and does not
promote any qualification or release state.

### Motivation

The first Session 55 affected transcript was valid but preceded the explicit complete-bundle
integration correction. The final ledger must distinguish the sidecar-focused unit seam from the
stronger generic-manifest-coherent integration case and retain a fresh, non-overwritten regression
identity.

### Root cause and evidence boundary

The unit RED→GREEN fixture rewrote the sidecar and related fields coherently, but it intentionally
did not claim to be a full generic-manifest fixture. The integration case now copies the complete
qualified-real-shaped bundle, adds the alternate executable to the manifest, recomputes all listed
checksums, and observes `MANIFEST_STATUS=verified` with `MANIFEST_FILE_COUNT=19` before semantic
rejection of the wrapper fixed-requested-path mismatch. The independent audit transcript is
`logs/i55-independent-requested-path-audit-20260720.log` (9,617 bytes,
SHA256 `41a9a2b7d9ea8138096965093c68afcdae8db2670070f2fd1cfc83e3b8fe0118`). Its canonical/hash-only
synthetic substitution observation remains the known authority blocker, not a new regression.

### Remediation and verification

No production logic beyond the already-reviewed requested-path comparison was changed. The final-v2
fail-fast matrix was run without overwriting the initial transcript and returned exit `0`; its log is
`logs/i55-current-affected-regression-final-v2-20260720.log` (7,650 bytes,
SHA256 `a78ea30c22667747d7d6f7a978c65fcda22b4b56b3a328e827f9b0b97d18ee84`). Actual values were
interpreter `PASS_COUNT=12`, alternate negative=`1`, parser negatives=`11`, duplicate-key
negatives=`4`, sidecar-tamper negatives=`9`, evidence-mode `PASS_COUNT=5`, and `94 passed in
4.75 s` for the artifact/sealer/package pytest subset; shell syntax, `git diff --check`, and pinned
Echo cleanliness all returned `0`.

### Residual disposition

The synthetic generic verifier's acceptance is not worker authenticity. Until an authority-approved
immutable interpreter/image digest and canonical-worker live evidence are supplied, keep Gate B1
`BLOCKED`, `real_pre_dataset` and `release_pre_dataset` `NOT QUALIFIED`, `AE-ready=NO`, and the
known `live_required=0` canonical/hash authority gap open. I56 marker/pointer/trusted-root/TOCTOU
work remains separate.

## Session 55 root-cause correction for alternate-manifest evidence — 2026-07-20

**Status:** Resolved as a documentation/test-evidence defect; no production semantic defect was
introduced and no qualification gate moved. I55 remains `OPEN / HIGH / BLOCK`.

### Symptom

The complete alternate requested-path integration was described as having a `19`-file generic
manifest, but the first durable transcript used for that claim only showed `MANIFEST_FILE_COUNT=13`.
The `13` lines were emitted by the surrounding valid integration fixture. The alternate fixture's
own count was not printed, so the transcript was insufficient evidence even though the fixture
itself contained the expected unique entries.

### Root cause

The integration test asserted semantic rejection after invoking the generic verifier but did not
emit a distinct status/count marker for the alternate bundle. Reviewers therefore had to infer the
`19`-file result from test setup rather than from the alternate invocation's durable output. This
was an evidence-output omission, not a failure of the generic manifest implementation: the
alternate executable was listed in the manifest and all listed checksums were recomputed coherently.

### Minimal resolution and verification

- The RED transcript `logs/i55-alternate-manifest-marker-red-20260720.log` is retained (257 bytes,
  SHA256 `8e50f7954f787344114fc1ccad74d85287482eb48f615f416e247fd67cfb4399`); it fails because the
  explicit alternate marker is absent.
- The integration contract now prints and asserts
  `ALTERNATE_MANIFEST_STATUS=verified` and `ALTERNATE_MANIFEST_FILE_COUNT=19`.
- The latest GREEN transcript is
  `logs/i55-qualified-alternate-path-integration-green-v3-20260720.log` (1,340 bytes,
  SHA256 `70006af6138aa16b40d93c15409f42c96517e6f75f309f787c397f82dfb4f8d1`). The v2 transcript
  (1,305 bytes, SHA256 `22b45f5aa77da46858c3c08b8c48809d491eb76a9259d21f109febef3aed8a76`) remains an
  intermediate record.
- The final affected regression is
  `logs/i55-current-affected-regression-final-v5-20260720.log` (7,801 bytes,
  SHA256 `1ded471a0304d85300822f92ff6e717d842b9ea2ba69ee2bd998bae65a83eb32`), with
  `FINAL_V5_EXIT=0`, `94 passed in 4.37s`, and both alternate-manifest markers present.

The old `i55-qualified-alternate-path-integration-green-20260720.log` remains immutable historical
evidence (1,272 bytes, SHA256 `cfda836abc419db5fa7a3bffe4eea6c861bdb0065f4810a6c790d33b715c6a37`) but is
explicitly not evidence for the alternate `19`-file count. The current integration source SHA256
is `1722e4d5d15be761a8eb4a81c37375421d12672634bf2cad825e1c76f8476578`.

### Residual boundary

The correction does not alter `SC26-AE/lib/task2_echo.sh`, the fixed requested-path literal, the
`live_required=0` canonical/hash authority gap, or any marker/pointer lifecycle. Generic-manifest
verification and semantic rejection are now independently observable, but neither proves an
approved immutable interpreter/image digest, canonical worker identity, external issuer, H800
qualification, real pre-dataset, release pre-dataset, or `AE-ready` state. Keep I53/I54/I56/I57/I58
and CR-01 at their existing open/partial dispositions.

### I59. V21 verifier required evidence from a gitignored log directory

**Status: RESOLVED / LOCAL.** This resolution covers only clean-clone provenance for the local
producer checkpoint and does not change any external qualification status.

**Root cause:** `tests/integration/sc26_ae_v21_verifier.py` verifies the summary's supplemental
identity fence, eight fixed evidence-marker logs, and the sole current verifier identity. The
required files resolve under `task_memory/task_2026-07-15_sc26_ae_workflow/logs/`, but the
repository-level `logs/` ignore rule excludes that directory. The local controller therefore
passes V21 using files that a clean clone cannot contain unless the exact required set is tracked.

**Independent finding:** StepCode Claude returned `BLOCK` in
`.omx/artifacts/claude-act-as-an-independent-read-only-reviewer-for-the-sc26-artifa-2026-07-20T04-34-12-767Z.md`.
The pre-reconciliation inventory resolved `65` unique required files: `6` task-root documents and
`59` logs totaling `228,172` bytes. Tracking only the old current verifier log would not close the
other supplemental dependencies.

**User decisions:** D31 approves tracking exactly the V21-required logs while preserving the
global ignore rule for unrelated logs. D32 approves deletion of the reviewer-generated nested
`.omc/` runtime state. The transient path was removed; nested tracked/staged/untracked status is
empty, and nested HEAD equals the outer gitlink at
`39755169f73f6c748e8d7376c3a2158c6569436b`.

**Closure evidence:** The exact required-log staged tree
`6c5cf790c62b021e1504621ae7489986a29990ec` was exported into a tracked-only repository and
committed ephemerally as `26f89b4df53760df8c38ac9ab62bfcf4ff0d6349`. Strict V21 exited `0`
with artifact/document rows=`7/10`, supplemental identities=`64`, shell=`47/47`, Python=`36/36`,
runtime-output exclusion=`1`, and `git diff --check=PASS`. This proves the approved remediation from
tracked bytes and closes I59 locally. Current-identity regeneration, exact-log restaging, final
staged-tree replay, the local Lore commit, and actual committed-clone verification remain mandatory
commit mechanics. I53/I54/I55/I56, Gate B1, both pre-datasets, and `AE-ready` retain their existing
open/blocked/not-qualified dispositions.

**Candidate remediation evidence:** Exact staging resolved required/staged logs=`59/59` with
missing/extra=`0/0`; unrelated top-level logs tracked/not-ignored=`0/0`; no runtime output, runtime
state, credential, high-confidence secret, or blob over `5 MiB` was staged. The initial tracked
snapshot exposed a second root cause: six ignored `SC26-AE/output/_work/.../source/*.sh` copies had
contaminated the local V21 shell count. A unit RED→GREEN repair now excludes runtime output at the
collector boundary, not by weakening syntax checks. Candidate tree
`841042300c32dc737d429feb333283fb53f7fbd0` reproduced V21 with exit `0`, shell=`47/47`,
Python=`36/36`, and `RUNTIME_OUTPUT_SHELL_EXCLUDED=1`.

**Follow-up review:** StepCode Claude returned `APPROVE` in
`.omx/artifacts/claude-act-as-an-independent-read-only-reviewer-for-the-sc26-ae-loc-2026-07-20T06-40-51-467Z.md`.
It found no CRITICAL/HIGH local blocker and accepted both the task-scoped archive whitespace
attribute and runtime-output exclusion as root-cause fixes. The subsequent penultimate
tracked-snapshot replay completed with exit `0`, so I59 is `RESOLVED / LOCAL`. The authoritative
hashes and sole current V21 identity must still be regenerated after this ledger update and replayed
from the final staged tree before the local commit is created.

## I60 — Unified Task1/Task2 source compatibility rejected unchanged Task2 producer — RESOLVED

**Root cause:** Task1 and Task2 were evaluated by the same simulator-only path allowlist. Dense
Task1 tracing changes are correctly incompatible with old Task1 artifacts but irrelevant to a
Task2 predictor whose exact Echo commit and load-bearing wrapper/tool blobs are unchanged.

**Resolution:** Task1 retains the strict allowlist. Task2 now has a separate conjunction-only source
contract plus the existing artifact verification. Focused tests pass `14/14`; the real two-GPU
artifact resolves as `task2_producer_equivalent_reuse`; independent verdict is `APPROVE`.

## I61 — Synthetic Task3 integration Qwen fixture contains one trace — OPEN / TEST-ONLY

The current product requires the 32-rank Qwen PP×EP representative vector, while
`tests/integration/test_sc26_ae_task3_contract.sh` still creates a one-trace prebaked fixture. The
same failure reproduces from an untouched `9baafdf` clean clone, so it is not caused by the Task2
compatibility change. Per the user's direction not to spend substantial time on test harnesses,
this fixture alignment is deferred and does not replace real Fresh-chain evidence.

## I62 — Functional packager requires exact current commits — OPEN / DIRECT BLOCKER

`SC26-AE/tools/package_prebaked.py build-functional` still rejects verified artifacts produced by
compatible heterogeneous commits. This must be fixed only after GPT Fresh Task3 closes, preserving
each source artifact's real producer identity and all checksum/provenance gates.
